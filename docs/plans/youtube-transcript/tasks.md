# Tasks: youtube-transcript MCP plugin (v1, stdio-only)

> Plan: ./plan.md · Spec AC cited by ID as AC-N (`docs/specs/2026-08-01-youtube-transcript.md`)
>
> Revised after plan-review cycle 9 (architecture/security/performance, opus/high-effort) — the final cycle of `--max-cycles=9`, FAILed a third cycle in a row, escalated at the hard budget cap and resolved by a second `hard-case-arbiter` arbitration pass. See plan.md's Decisions Made and Verification & Sources for the full record. **Cycle 9 found cycle 8's own fixes had introduced four new criticals, three of them side effects of fixes for unrelated cycle-8 findings rather than recurrences of the same theme** — resolved via two rules, not four independent patches: **Rule A** (each component a consolidating fix moves must be individually checked against `ALLOWED_EDGES`/wave order/spec, not just the bundle's new call site) applied to (1) `net.Transport` — cycle 8 bundled the opener into it alongside `sleep`/`jitter`, handing `providers/innertube.py` an object able to bypass every SSRF/scheme/host check `net.fetch()` enforces; `Transport` now carries only `sleep`/`jitter`, the opener stays private to `net/client.py`; (2) the `Accept-Encoding`/`User-Agent` header fix — cycle 8 had `net/client.py` (Wave 0/1) consume `USER_AGENT` from `server.py` (Wave 3), an impossible edge and wave order that also contradicted the spec's own already-recorded decision that outbound UA is the ANDROID/IOS client string; `net/client.py` now sends only `Accept-Encoding: gzip` unconditionally, `providers/innertube.py` (T-10) sets `User-Agent` on its `headers`. **Rule B** (the content-boundary mechanism's wire carrier is named explicitly, and any layout change updates the spec in the same diff) applied to: cycle 8's "separate `contentNotice` field" fix was verified only against `build()`'s in-memory dict, not the real MCP wire format — this repo's pattern serializes the whole result with one `json.dumps()` into `content[0].text` (confirmed against `maven-mcp/plugin/server/server.py:11510`), so the two dict keys still concatenated on the wire and the original 3-vs-2 counting bug reappeared; T-13a's `Registry.call()` now names this serialization step explicitly, and AC-26's reference parser does `json.loads()` first, then searches only the parsed dict's transcript key — plus the spec (AC-15/AC-26) is synchronized to this design in the same revision, closing a desync cycle 8 introduced by editing the plan without editing the spec. Per the arbiter's process recommendation, the next verification step is a narrow, targeted check of these four areas, not a tenth full-panel cycle — before moving to implementation. **That narrow check (security-expert, opus, scoped to exactly the four cycle-9 fix areas) ran and returned CONDITIONAL PASS: all four fixes were structurally correct, but each area's regression guard was weaker than the fix itself** — findings Y-1 through Y-17, all applied in this revision: the `.opener` text-ban was written too broadly (`\.open(` matched unrelated stdlib calls) and too narrowly scoped (excluded `tests/`, where it needed to *not* apply, since tests must mock the opener) — narrowed to `\.opener\b`, production code only; `Transport`'s no-opener test checked one named field instead of the field set, replaced with a set-equality assertion; `net.Response` (the border object flowing the other direction, `net/` → `providers/`) had never been described at all — pinned as a plain frozen data container alongside `Transport`; the `Accept-Encoding`/`User-Agent` fix's header-merge direction let a caller override the "unconditional" base header — inverted so the base wins; AC-26 was still being verified against a `json.dumps()` call made *inside the test*, not the real wire bytes `protocol/dispatch.py` produces — the actual AC-26-closing test now dispatches through the real stack (T-14); the wrapped-transcript dict key was never given a literal name — now `transcript`, with `FIELDS_BY_STATUS[Status.ok]` frozen to match; plus a set of cheap misc/nit fixes (Y-4, Y-7, Y-8, Y-11, Y-13, Y-14, Y-15, Y-17) and one new task, T-P4, closing a set of plan/spec divergences the narrow check found accumulating since cycle 3. No further review cycle is planned — `/acceptance`'s per-task code-level review is the next verification this plan receives.
>
> **Cycle 8's fixes (unchanged, still in force)**: `ALLOWED_EDGES` symbol-placement rule narrowed to its one load-bearing disjunct; `HTTP_TIMEOUT` moves to `domain/` alongside `FORMATS`; `SERVER_VERSION`/`USER_AGENT` assigned to T-13b, `--check-tag` moved to T-16; `build()` raises `ValueError`, not `DomainFailure`, on its own invariant violation; `TrackListing`/`TrackDescriptor` gain `default_audio_language`/`is_default` for AC-2's tiers 2-3; T-11/T-12 gain an explicit normalize-before-open test; T-16 gains `plugin-dev:plugin-validator` + PLUGIN-STANDARDS §10; plus `RecursionError`/`ValueError` JSON-exception mapping, `HOST_TIMEOUT_MARGIN` quantified, T-P3's single-probe-first host-timeout reordering, `sanitize_text`'s `Cs` (surrogate) addition, `redact_url`'s userinfo/fragment stripping, upstream `baseUrl`'s port check, and `estimate_characters` computed once per `list_transcript_tracks` call rather than per track. **Performance-precision findings accepted for v1, not chased further** (see plan.md's Decisions Made and Technical Approach): the deadline margin's one-operation-per-attempt assumption, decode-phase peak memory, and per-page CPU-phase waste are documented as honest, nominal-case estimates rather than re-derived to a proven bound.

Wave 0 (serial): T-1 → T-2 → T-3. Wave 0 (parallel): T-P1, T-P2, T-P3, **T-P4** (narrow-check fix, Y-16 — no dependencies, must close before T-16). Wave 1 (parallel, after T-3): T-4, T-5, T-6a→T-6b, T-7, T-8, T-9, T-13a (`after: T-7`) — T-7 depends only on `T-3` (no dependency on `T-5`'s encoders; the content-boundary mechanism no longer touches format-specific text transforms at all). Wave 2 (parallel): T-10, T-11, T-12. Wave 3 (serial merge): T-13b. Wave 4: T-14, T-15 in parallel; T-16 (`after: T-14, T-P4`) follows.

---

## T-1 — Package skeleton, pyproject.toml, test harness shim
- after: none
- files: `plugins/youtube-transcript/plugin/server/{domain,net,providers,formats,protocol,tools}/__init__.py`, `plugins/youtube-transcript/pyproject.toml`, `plugins/youtube-transcript/tests/__init__.py`, `plugins/youtube-transcript/tests/_helpers.py` (sys.path shim + hermetic env-var reset only)
- interface: consumes none · produces an importable, empty package tree; `pyproject.toml` matching `plugins/maven-mcp/pyproject.toml`'s shape, repointed
- acceptance: THE SYSTEM SHALL have a package tree that passes `python3 -m compileall` with zero errors and a `pyproject.toml` all three tools load without configuration errors
- check: `python3 -m compileall plugins/youtube-transcript/plugin/server`; `ruff check ...`; `mypy --config-file plugins/youtube-transcript/pyproject.toml`

## T-2 — `domain/` complete
- after: T-1
- files: `plugins/youtube-transcript/plugin/server/domain/__init__.py`, `plugins/youtube-transcript/tests/test_domain.py`
- interface: consumes none (stdlib only) · produces `Status(str, Enum)` (13 members, unchanged: `ok`, `no_transcript`, `language_unavailable`, `video_not_found`, `video_unavailable`, `age_restricted`, `region_blocked`, `live_not_ready`, `rate_limited`, `blocked_by_provider`, `upstream_changed`, `transcript_too_large`, `transport_error`); `DomainFailure(Exception)` (common base for `ProviderError`, `DeadlineExpired`, `CursorInvalid`, and `TrackFieldInvalid`, letting `protocol/` distinguish domain-modeled failures from bugs via its already-allowed `domain/` edge, without needing `providers/base.py`; see T-13a); `TrackFieldInvalid(DomainFailure)` (raised by `TrackDescriptor.__post_init__` when `language_code`/`kind` fail their validation pattern, or `languageName` exceeds its length bound — **cycle 7 addition to the same field, see acceptance**; maps to `upstream_changed` via `outcome_from_error`, same as `CursorInvalid`); `CursorInvalid(DomainFailure)` (**moved here from `tools/cursor.py`, cycle 7 — symbol-placement rule**: raised by `tools/cursor.py::decode()`, T-8, but declared in `domain/` so T-3's/T-14's totality tests see it independent of Wave-1 completion — the same reasoning that already applies to `TrackFieldInvalid`); `Segment`, `Transcript`, `TrackDescriptor` (`track_id: str`, cycle 3 — populated by the provider via `encode_track_id`; **`estimated_characters: Optional[Mapping[str, int]]` with keys `{text, srt, vtt}`, cycle 4 fix** — AC-1 requires an object, not a scalar; `None` until `tools/list_transcript_tracks.py` populates it for the response only, see T-10/T-11's ownership split; **`language_name: str`, `__post_init__` now also enforces `len(language_name) <= 100`, raising `TrackFieldInvalid`, cycle 7 fix** — `languageName` is upstream-controlled and already treated as untrusted (T-10 runs it through `sanitize_text`), but had no length bound, unlike `language_code`/`kind`; **`is_default: bool`, cycle 8** — populated by the provider from the player response, feeds AC-2's tier-3 resolution in T-9), `TrackListing` (`tracks: Tuple[TrackDescriptor, ...]` — may legitimately be an empty tuple, see T-10/T-11's redesign; `duration_seconds: int`; **`default_audio_language: Optional[str]`, cycle 8** — both explicitly declared here, cycle 8 fix, since T-10/T-11 already read `duration_seconds` and AC-2's tier-2 resolution needs `default_audio_language`, neither of which had an owning declaration before), `VideoId`, `ToolOutcome`, `Deadline` (`.start(budget, clock=time.monotonic)`, `.remaining()`, `.expired()`), `DeadlineExpired(DomainFailure)`, `STATUS_POLICY`, `generate_content_boundary() -> str` (**replaces the fixed `CONTENT_SENTINEL` constant entirely, cycle 6 escalation — spec AC-15 redesigned**: returns a fresh value each call, `f"<<<UNTRUSTED_CONTENT_{secrets.token_hex(16)}>>>"` — 128 bits of CSPRNG entropy, generated at `protocol/envelope.build()` time, per invocation, never a module-level constant. The prior fixed-sentinel design required every text-transform touching transcript content to correctly and adversarially-robustly avoid reconstructing a *known* string — six review cycles found a genuine defect in that obligation at a different site each time (non-idempotent removal in two separate places, a removal step forging a new dangerous token, a sibling AC-21 transform sharing the identical non-idempotency defect independently). A per-call random value cannot be predicted by an attacker whose caption text was authored before the call happens, closing the forgery concern by construction — removing the entire scrubbing obligation, not the day's instance of it — **but not, by itself, the verifiability half; see T-7's `content_notice()`, cycle 7, and its cycle 8 field-separation fix**), `FORMATS: FrozenSet[str] = frozenset({"text", "srt", "vtt"})` (**moved here from `formats/__init__.py`, cycle 7 — symbol-placement rule**: sole consumer without a permitted edge to `formats/` is `protocol/schemas.py` (T-7) — `tools/` already has that edge and needs no relocation to reach it, corrected justification cycle 8; `formats/__init__.py` re-exports it, same treatment as `MAX_SEGMENTS`), `MAX_SEGMENTS = 100_000` (moved here from `formats/__init__.py`, cycle 3 — three consumers in three packages, `providers/innertube.py` can only reach it from `domain/`), `DEADLINE_CHECK_STRIDE = 1000` (cycle 3 — pins the mid-pass deadline-check interval named but unquantified in cycle 2's fix), `TOOL_CALL_DEADLINE`/`CPU_PHASE_BUDGET`/`ENCODE_RESERVE`/**`HTTP_TIMEOUT`** (moved here from `net/client.py`; `TOOL_CALL_DEADLINE`/`ENCODE_RESERVE` cycle 4, `CPU_PHASE_BUDGET`/`HTTP_TIMEOUT` cycle 7/cycle 8 — describe the whole-invocation `Deadline` budget or feed its derived reservation, not the network layer specifically; **`HTTP_TIMEOUT = 15` moves here cycle 8** — `ENCODE_RESERVE = HTTP_TIMEOUT + CPU_PHASE_BUDGET = 35` is computed in `domain/` and needs both operands importable in-package, which `HTTP_TIMEOUT` living in `net/client.py` made impossible (`domain/`'s `ALLOWED_EDGES` row is `nothing`); `net/client.py` re-exports `HTTP_TIMEOUT` from `domain/` for its own socket-level calls, and otherwise keeps only `HTTP_MAX_ATTEMPTS`/`MIN_ATTEMPT_TIMEOUT`/`HTTP_MAX_RESPONSE_BYTES`), `HOST_TIMEOUT_MARGIN = 30` (**new, cycle 8** — quantifies the previously-unquantified `margin` referenced by T-16's gate 4 and T-P3's stop condition; sized as roughly one worst-case leg plus CPU-phase headroom), `sanitize_text(s)->str` (**scope widened, cycle 7, then cycle 8**: strips `Cc` ∪ `Cf` ∪ **`Cs`** ∪ `{U+2028, U+2029}` minus `\n`/`\t` — not only C0/C1, per AC-21's widened wording; cycle 7 added DEL/bidi-override/invisible-formatting characters (U+202A–U+202E, U+2066–U+2069, U+FEFF); **cycle 8 adds the `Cs` (surrogate) category** — an unpaired surrogate codepoint from a malformed upstream response can survive `json.loads` and, if `json.dumps(..., ensure_ascii=False)` is ever used, raise `UnicodeEncodeError` writing to stdout, which in this server's single-threaded stdio loop would abort the whole process mid-response rather than one call. Translation table built once as a **module-level constant**, not reconstructed per call — no sentinel-related logic of any kind, the stack-based algorithm/fixed-point loop/cap-and-`ValueError` from the removed design don't exist here), `redact_url(url:str)->str` (`urlsplit(u)._replace(query="", fragment="", netloc=parsed.hostname or "").geturl()` — moved here from `net/` so both `net/` and `providers/` can use it; **cycle 8: also strips `fragment` and reduces `netloc` to the bare hostname**, closing a gap where userinfo credentials embedded in a URL would have survived the previous query-only redaction)
- acceptance: `STATUS_POLICY` has exactly one entry per `Status` member, `isError` `False` in every row; `DeadlineExpired` is a `DomainFailure` subclass; `Deadline.remaining()` decreases monotonically under an injected clock, `.expired()` becomes `True` once elapsed; `sanitize_text` strips control chars across the widened `Cc ∪ Cf ∪ Cs ∪ {U+2028, U+2029}` set (preserving `\n`/`\t`), tested against a fixture containing embedded C0/C1, DEL, bidi-override, and an unpaired surrogate codepoint (AC-21's control-char clause) — no sentinel-related assertion of any kind; `generate_content_boundary()` called twice returns two different values (proves it isn't a disguised constant) and each individual value matches `^<<<UNTRUSTED_CONTENT_[0-9a-f]{32}>>>$`; `redact_url` strips a URL's query string, fragment, and userinfo (tested against `https://user:token@host/path?q=1#frag`, asserting the redacted form contains none of `user`, `token`, `q=1`, or `frag`); `TrackDescriptor.__post_init__` raises `TrackFieldInvalid` for a `language_name` longer than 100 characters; `ENCODE_RESERVE == HTTP_TIMEOUT + CPU_PHASE_BUDGET` asserted directly as a derived-value invariant, importing both operands from within `domain/` itself (not restated as a separate literal, and not requiring any cross-package import to construct)
- check: `test_domain.py::{test_status_policy_totality,test_deadline_expired_is_domain_failure,test_deadline_injected_clock,test_sanitize_text_strips_control_chars_widened_set,test_generate_content_boundary_is_random_and_well_formed,test_redact_url_strips_query_fragment_and_userinfo,test_language_name_length_bound_rejected,test_encode_reserve_is_derived_in_domain}` (closes AC-17's totality, AC-10's `isError` totality, AC-21's control-char clause)

## T-3 — Contracts freeze: session-scoped provider port, `net`/`formats` constants, AST import-boundary test
- after: T-2
- files: `plugins/youtube-transcript/plugin/server/providers/base.py` (full: `TranscriptProvider`/`ProviderSession` ABCs, 10-member `ProviderError(DomainFailure)` hierarchy — `NoTranscript` removed, `trackId` encode/parse), `plugins/youtube-transcript/plugin/server/net/client.py` (constants + exception classes only — `TOOL_CALL_DEADLINE`/`ENCODE_RESERVE`/`HTTP_TIMEOUT` moved to `domain/`, cycle 4/cycle 8, re-exported here), `plugins/youtube-transcript/plugin/server/formats/__init__.py` (constants only — `MAX_SEGMENTS`/`FORMATS` imported from `domain/`, not defined here — cycle 3/cycle 7), `plugins/youtube-transcript/plugin/server/tools/__init__.py` (`outcome_from_error()` skeleton, `Union[ProviderError, DeadlineExpired]`), `plugins/youtube-transcript/tests/_helpers.py` (extend: `FakeProvider`/`FakeSession` with `fetch(track, deadline)`, `mock_urlopen`, `http_error`, call-counting transport double), `plugins/youtube-transcript/tests/test_source_policy.py` (**owned here, cycle 8 — this task's `check` already referenced `test_dunder_import_banned_via_ast_name_check` in this file without listing the file itself; T-6a/T-14 extend it with their own portions, T-3 creates it with the `__import__`-AST-Name check**), `plugins/youtube-transcript/tests/test_import_boundaries.py` (AST-based, **further hardened per cycle 4's confirmed bypasses of the cycle-3 fix**: `ast.walk(tree)` not `tree.body`; relative imports resolved to absolute dotted names before comparison, explicit `node.module is None` branch; for every `ImportFrom` node, the restricted-name check tests **both** `node.module` **and** `f"{node.module}.{alias.name}"` per alias (cycle 4 finding — checking `node.module` alone lets `from urllib import request`/`from http import client` through undetected, since neither is `"urllib.request"`/`"http.client"` as a bare `node.module` string); the same-package-always-allowed rule (with one exception, see below) plus the cross-package-only `ALLOWED_EDGES` table per plan.md, unlisted-module-fails rule; **two committed static stdlib-name lists, one per CI matrix leg** (`tests/fixtures/stdlib_names_3_9.json`, `tests/fixtures/stdlib_names_3_13.json`) for AC-14 (see acceptance for why one list generated on one interpreter version doesn't work), not a runtime version check; `urllib.request`/`urllib.error`/`http.client`/`socket`/`ssl`/`ftplib`/`asyncio`/`ctypes`/`xmlrpc.client`/`smtplib`/`webbrowser`/`http.server` restricted to `net/client.py` only; `importlib` banned in **all** of `plugin/server/**` including `net/client.py` (`net/client.py` is the one module where dynamic egress-bypassing import is most dangerous, and nothing in this plan ever needs it there) — `__import__` is a separate, AST-Name-based check in `test_source_policy.py` (see acceptance), not part of this file's `ast.Import`/`ast.ImportFrom` scan; bare `import urllib`/`import urllib.parse` banned outside `net/client.py` (mandating `from urllib.parse import urlsplit`) — **explicitly documented as hygiene/regression coverage, not a hard security boundary** (cycle 4 finding: a module holding any reference into `sys.modules` can still reach `urllib.request` once `net/client.py` has imported it, via `sys.modules["urllib.request"]` or `some_module.__package__`-style lookups that touch no `urllib` name in source at all — no static source-level check closes this in a single-process Python program; the actual, load-bearing guarantee that egress stays in `net/client.py` is code review, and this AST check's job is only to catch accidental/careless violations early and cheaply, stated as such rather than oversold); xml-import-restricted-to-`net/client.py` check; one self-test per bypass class (function-nested import, relative import, `importlib.import_module`, `from urllib import request`, `from http import client`, attribute-access via a sibling's `urllib.parse` import) — **always green from creation**; `providers/base.py` may **not** import `providers/innertube.py` (cycle 4 finding — the general same-package rule accidentally legalized the abstraction importing its own implementation, the one direction `composition.py`'s existence as a separate wiring layer exists to prevent; one negative-direction self-test)
- interface: consumes `Status`/`DomainFailure`/`Segment`/`Transcript`/`TrackDescriptor`(incl. `track_id`/`estimated_characters`/`is_default`)/`TrackListing`(incl. `duration_seconds`/`default_audio_language`)/`VideoId`/`Deadline`/`DeadlineExpired`/`generate_content_boundary`/`MAX_SEGMENTS`/`DEADLINE_CHECK_STRIDE`/`TOOL_CALL_DEADLINE`/`ENCODE_RESERVE`/`HTTP_TIMEOUT`/`HOST_TIMEOUT_MARGIN` from T-2 · produces `TranscriptProvider(ABC)` with `normalize_video_ref(value:str)->Optional[VideoId]`, `open(video_id:VideoId, deadline:Deadline)->ProviderSession`; `ProviderSession(ABC)` with `.listing:TrackListing` (tracks may be empty), `.fetch(track:TrackDescriptor, deadline:Deadline)->Transcript` (deadline is an explicit per-call parameter — raises `UpstreamChanged` if `track` is not a member of `self.listing.tracks` — precondition stated here, tested at T-10); `ProviderError(DomainFailure)` (cycle 4 — was `ProviderError(Exception)`) + 10 subclasses (`VideoNotFound`, `VideoUnavailable`, `AgeRestricted`, `RegionBlocked`, `LiveNotReady`, `RateLimited`, `BlockedByProvider`, `UpstreamChanged`, `TranscriptTooLarge`, `TransportError`), each `status: ClassVar[Status]` + optional `retry_after`; `encode_track_id`/`parse_track_id`; `net/client.py`'s `HTTP_MAX_ATTEMPTS=2`, `MIN_ATTEMPT_TIMEOUT=5`, `HTTP_MAX_RESPONSE_BYTES=10*1024*1024` (`TOOL_CALL_DEADLINE`/`ENCODE_RESERVE`/`HTTP_TIMEOUT` moved to `domain/`, cycle 4/cycle 8 — this list no longer defines them, though `net/client.py` re-exports `HTTP_TIMEOUT`), `net.Transport` dataclass (**new, cycle 8, corrected cycle 9, `frozen=True` narrow-check fix** — bundles injectable `sleep`/`jitter` **only** (never the opener, which stays private to `net/client.py` — see T-6a), replacing the two bare parameters previously threaded separately through `InnertubeProvider.__init__`/`InnertubeSession`); `net.Response` dataclass (**new, narrow-check fix (Y-3)** — `@dataclass(frozen=True)` with exactly three fields, `status: int`, `headers: Mapping[str, str]`, `body: bytes`; `fetch()`'s return type across the `net/` → `providers/` border, previously undescribed in any `produces`/`ALLOWED_EDGES` discussion despite crossing the identical border `Transport` does — a naive `fetch()` returning the raw `http.client.HTTPResponse`/`urllib` response object would hand `providers/innertube.py` a live `.read()`/`.fp` handle, the same class of risk `Transport`'s opener capture was, just unnoticed because it wasn't named); exception set `NetError`/`ResponseTooLarge`/`TransportFailed`/`Throttled`/`PolicyRejected`/`BlockedUpstream`/`MalformedUpstream`; `formats/__init__.py`'s `MAX_PAGE_CHARS=50_000`/`MAX_PAGES=20`/`CHARS_PER_SECOND=15`/`CUE_OVERHEAD_FACTOR=3` (`MAX_SEGMENTS`/`FORMATS` re-exported from `domain/`, not defined here — cycle 3/cycle 7); `tools/__init__.py`'s `outcome_from_error(exc: DomainFailure) -> ToolOutcome` (**type broadened from `Union[ProviderError, DeadlineExpired]`, cycle 6** — now accepts any `DomainFailure`, so `tools/cursor.py`'s `CursorInvalid(DomainFailure)` is covered by the same function and the same totality test with zero special-casing); `FakeProvider`/`FakeSession` test doubles
- acceptance: `parse_track_id` returns `None` for any value not matching `^(manual|auto):[A-Za-z0-9-]{1,20}$`; `outcome_from_error` maps every one of the 10 `ProviderError` subclasses, `DeadlineExpired` (→ `transport_error`), **and `CursorInvalid` (→ `upstream_changed`, cycle 6)** to a `ToolOutcome` whose `status` is present in `STATUS_POLICY` (totality test, covering all `DomainFailure` subclasses uniformly); `test_import_boundaries.py` asserts, via `ast.walk` over every file under `plugin/server/**`, that (a) each module's imports (own-package always allowed except the `providers/base.py`→`providers/innertube.py` exception above, cross-package per the table) are a subset of `ALLOWED_EDGES` **and any module not listed fails the test**, (b) every top-level stdlib-external import name is absent — **mechanism, cycle 5 fix**: **two** static lists of stdlib module names, one per CI matrix leg (`tests/fixtures/stdlib_names_3_9.json`, `tests/fixtures/stdlib_names_3_13.json`), each generated once **on that exact interpreter version** and committed, selected at test time via `sys.version_info` (cycle 5 finding — cycle 4's mechanism specified generating the 3.9 list "from `sys.stdlib_module_names` on a 3.10+ interpreter", which is self-contradictory and verified empirically wrong: the 3.9 and 3.10+ stdlib sets differ by over 20 module names in both directions — e.g. `distutils`/`smtpd`/`crypt` are 3.9 stdlib but absent from a list generated on 3.10+, while `tomllib` is the reverse — so a list generated the way cycle 4 specified would silently misclassify pip packages sharing either set's names on whichever leg it doesn't match; since `sys.stdlib_module_names` doesn't exist before 3.10 at all, the 3.9 list must be produced by a different method actually run on 3.9, e.g. `sys.builtin_module_names` unioned with every top-level name importable from a bare 3.9 stdlib install with no pip packages present) — no `skipUnless`, no `sysconfig.get_paths()` fallback (cycle 4 found that fallback's `purelib`/`site-packages` path is nested inside `stdlib` on the standard `posix_prefix` layout `actions/setup-python` actually uses, so it silently passed every pip package; separately, builtins like `sys`/`time`/`itertools` have no `__file__` at all and crashed or false-failed it) — both checked-in lists are regenerated only as a deliberate, reviewed step, never at test time, (c) `urllib.request`/`urllib.error`/`http.client`/`socket`/`ssl`/`ftplib`/`asyncio`/`ctypes`/`xmlrpc.client`/`smtplib`/`webbrowser`/`http.server` are imported nowhere except `net/client.py` (cycle 5 — expanded from the original six; these six were also egress- or dynamic-code-capable and unrestricted), `importlib` is imported nowhere at all (including `net/client.py`), the combined-name `ImportFrom` check (module **and** module.alias) catches `from urllib import request` and `from http import client` outside `net/client.py`, and bare `import urllib`/`import urllib.parse` is absent outside `net/client.py` (source-text ban in `test_source_policy.py`, defense-in-depth — see the files entry above for why this is hygiene, not a closed security boundary); **`__import__` usage is checked separately, in `test_source_policy.py`, via an AST walk for `ast.Name(id="__import__")` nodes — not as part of this file's `ast.Import`/`ast.ImportFrom`-based scan** (cycle 5 finding — `__import__` is a builtin function call, `Name`/`Call` nodes in the AST, never an `Import`/`ImportFrom` node; a prior version of this task claimed it was caught by the same mechanism as `importlib`, which cannot see it at all and would have been vacuously green forever; the AST-Name form, not a text grep, avoids false-flagging the string `__import__` inside a comment or docstring), (d) any `xml.*` module is imported nowhere except `net/client.py` — all checks pass trivially on the tree built so far and stay green as later waves add code. **The cross-package edge self-check against actual code (cycle 3's "every constant named in a task's `interface` resolves to a permitted module") moves to T-14** (cycle 4 finding — as a check of `tasks.md` prose it was vacuous at T-3, before any consuming code existed; T-14 runs after Wave 3 when the full module tree is built, so it can walk real imported-symbol resolution instead of parsing plan documents)
- check: `test_providers_base.py::{test_track_id_roundtrip_and_rejection,test_provider_error_is_domain_failure,test_outcome_from_error_totality}`, `test_import_boundaries.py::{test_unlisted_module_fails,test_function_nested_import_caught,test_relative_import_resolved,test_importlib_banned_everywhere,test_from_urllib_import_request_caught,test_from_http_import_client_caught,test_attribute_access_bypass_documented_not_closed,test_providers_base_cannot_import_innertube,test_stdlib_check_against_committed_per_leg_lists}`, `test_source_policy.py::test_dunder_import_banned_via_ast_name_check` (referenced by every later task's `check`) (closes AC-18, AC-10's translation ownership, AC-14, AC-24 as amended by the fast-follow spec addendum — see plan.md's Verification & Sources)

## T-4 — `providers/video_ref.py`
- after: T-3
- files: `plugins/youtube-transcript/plugin/server/providers/video_ref.py`, `plugins/youtube-transcript/tests/test_video_ref.py`
- interface: consumes `VideoId` from T-2 · produces `normalize(value:str) -> Optional[VideoId]`
- acceptance: full AC-6 URL-form table plus both attack payloads (`@evil.com`, `youtube.com.evil.com`) return `None`, host matched via `urlsplit(...).hostname` exact equality only
- check: `test_video_ref.py` (closes AC-6)

## T-5 — `formats/{text,srt,vtt}.py`
- after: T-3
- files: `plugins/youtube-transcript/plugin/server/formats/text.py`, `formats/srt.py`, `formats/vtt.py`, `formats/__init__.py` (extend with dispatch), `plugins/youtube-transcript/tests/test_formats_text.py`, `test_formats_srt.py`, `test_formats_vtt.py`
- interface: consumes `Transcript`/`Segment`/`Deadline`/`DeadlineExpired`/`MAX_SEGMENTS`/`FORMATS`/`DEADLINE_CHECK_STRIDE` from T-2 (**`FORMATS` moved to `domain/`, cycle 7 — see T-2; `formats/__init__.py` re-exports it, this task consumes rather than owns it now**), `MAX_PAGE_CHARS`/`CHARS_PER_SECOND`/`CUE_OVERHEAD_FACTOR` from T-3 · produces `encode(transcript, fmt, *, start_index=0, max_chars=MAX_PAGE_CHARS, options, deadline:Deadline) -> Page`; `count_pages_to(transcript, fmt, *, target_index, max_chars, options, deadline:Deadline) -> int` (single O(segments-so-far) pass summing encoded lengths, never materializing page text — checks `deadline.expired()` every `DEADLINE_CHECK_STRIDE` segments **during** the pass, not only at entry); **`estimate_characters(duration_seconds: int) -> Mapping[str, int]`** (cycle 5 finding — previously named only in prose ("`estimate_characters` formula") with no declared signature or owning file, while T-11 already `consumes estimate_characters from T-5` as if it existed; one aggregating function in `formats/__init__.py`, not three per-format functions, per AC-1's formula: `{"text": round(duration_seconds × CHARS_PER_SECOND), "srt": text × CUE_OVERHEAD_FACTOR, "vtt": text × CUE_OVERHEAD_FACTOR}`)
- acceptance: byte-exact concat for `text`/`srt`; `vtt` per-page validity + two-line-header-strip concat; **cue-injection neutralization — a format-correctness measure, not a security boundary, per AC-21's redesign** (`docs/specs/2026-08-01-youtube-transcript.md` — the whole transcript is already wrapped in T-7's unforgeable per-call random boundary, so this transform no longer needs to be adversarially idempotent; it is implemented correctly anyway, cheaply, using a longest-match regex — `re.sub(r"-{2,}>", "->", text)` (**cycle 7 fix — the pattern is a real `>` character matching the literal substring `-->`, not the HTML entity `&gt;`; a prior revision of this task and plan.md itself had the regex and its test fixture both mistakenly written with `&amp;gt;` in place of `>`, which never matches real `-->` text and let the paired test pass vacuously — found by grepping the raw document bytes**) — which closes the one real correctness bug found for it (`"--->".replace("-->","->")` reconstructs `"-->"` under a naive single-token replace; the longest-match form doesn't have this defect, verified against `"--->"`/`"---->"`/`"-------->"` all collapsing to `"->"`), plus blank-line-collapse (a run of blank lines removed entirely, per AC-21's literal "to nothing"); order pinned as blank-line-collapse then arrow-collapse; unit-tested against a fixture with both an embedded blank-line-plus-timecode-like string and a real (not HTML-escaped) `--->`-style repeated-hyphen string, asserting the output contains no literal `-->` substring; **a property test asserts `collapse(collapse(x)) == collapse(x)` (idempotency) and "no `-->` substring in the output" against randomized strings drawn from `{-, >, \n, space}`, cycle 7 fix** — so the assertion cannot be satisfied by a no-op collapse function, closing the "test passes vacuously" failure mode the HTML-escaping bug produced; no idempotency-under-nesting test targeting an adversarial payload is required beyond the property test, since this is no longer a security-critical property; `estimate_characters` produces exactly the three-key object above, per AC-1's formula, for a known `videoDurationSeconds` fixture; GIVEN a `Deadline` that expires partway through a 100,000-segment pass (injected clock advancing per-segment) WHEN `count_pages_to`/`encode` is called THEN `DeadlineExpired` is raised having processed no more than `expiry_index + DEADLINE_CHECK_STRIDE` segments (position-based, stride-bounded assertion, not "approximately" — closes cycle 3's finding that "approximately" is satisfied equally by a stride of 1 or of 50,000); `count_pages_to` and iterating `encode` agree exactly on page boundaries (agreement test)
- check: `test_formats_text.py::test_byte_exact_concat`, `test_formats_srt.py::{test_byte_exact_concat,test_cue_injection_neutralized,test_cue_collapse_idempotent_property}`, `test_formats_vtt.py::{test_per_page_valid,test_two_line_header_strip_concat,test_cue_injection_neutralized}`, `test_formats_init.py::test_estimate_characters_three_key_object` (moved from per-format files, cycle 5 — `estimate_characters` is one function in `formats/__init__.py`, not three), shared `test_deadline_expires_within_stride_bound`, `test_count_pages_to_agrees_with_encode` (closes AC-1, AC-11, AC-21 part 2, AC-20's decode/encode deadline coverage)

## T-6a — `net/client.py` policy: scheme/host allowlist, TLS, redirect handling
- after: T-3, T-P3
- files: `plugins/youtube-transcript/plugin/server/net/client.py` (opener construction, allowlist, redirect-code inspection, **`Transport` dataclass — cycle 8, see below**), `plugins/youtube-transcript/tests/test_net_client_policy.py`, `plugins/youtube-transcript/tests/test_source_policy.py` (TLS-bypass-token bans — this task's portion, **plus the `Transport`-opener-access ban, cycle 9**)
- interface: consumes `Deadline` from T-2, constants/exceptions from T-3, confirmed host set from T-P3 · produces the policy half of `fetch()`; the opener and its `SSLContext` are **constructed once per process** (module-level private singleton, `_OPENER` — not per `fetch()` call — cycle 3 finding: `ssl.create_default_context()` loads the system CA bundle, costing ~10-30ms per call for nothing if rebuilt 3× per invocation; **never exposed outside `net/client.py`, cycle 9 — see below**); produces `Transport` (**new, cycle 8, corrected cycle 9, `frozen=True` narrow-check fix**) — a small **frozen** dataclass holding **only** `sleep: Callable[[float],None]` and `jitter: Callable[[float,float],float]`, constructed once by `InnertubeProvider.__init__` (T-10) and passed as a single object into `InnertubeSession`, replacing the two bare `sleep`/`jitter` parameters that were previously threaded separately through `net.fetch()`, `InnertubeProvider.__init__`, and `InnertubeSession` — a seam that had already broken at two different hops in cycles 6 and 7 because each intermediate layer had to remember to forward two loose parameters; bundling them into one object passed whole removes "a layer forgot to forward it" as a possible state (`frozen=True` — narrow-check fix — since T-6b shares one module-level default `Transport()` instance across call sites, and a mutable dataclass would let one caller's monkeypatch leak into another's). **`Transport` does NOT hold the opener, cycle 9 correction** — cycle 8's version did, which meant `providers/innertube.py` held an object with `.opener.open(url)`, able to reach the network bypassing every scheme/host/port/redirect check `fetch()`'s body enforces, with zero `ALLOWED_EDGES` violation (architecture found this in cycle 9); `fetch()` reads `_OPENER` directly and never receives it through `Transport`. `fetch()`'s return type, `Response` (see T-3's `produces`), is likewise a plain frozen data container — never the raw `http.client.HTTPResponse`/`urllib` object `fetch()`'s internals touch, so nothing crossing this same border on the way back out carries a `.read()`/`.fp` handle either (narrow-check finding — `Response` had gone undescribed while `Transport` was being fixed, the same border, the same risk class, just unnoticed because `fetch()`'s return path reads as "just data" without being pinned as such)
- acceptance: `scheme != "https"` rejected before the host check (`file:`/`data:`/`ftp:`/`http:` all rejected, verified the opener's `open()` call is never reached — mock the opener and assert zero calls, not just "an exception was raised", per cycle 2's finding that a weaker test could pass for the wrong reason); host pinned as `urlsplit(url).hostname`, `None`/empty host rejected (`https:///x` case) before any string comparison; **exact-match-only allowlist `{www.youtube.com, youtubei.googleapis.com}` — T-P3 confirmed no `*.googlevideo.com` host is used for caption delivery in this plugin's scope (`swarm-report/research/youtube-transcript-size-measurements.md`), so the suffix-match branch is dropped entirely rather than kept unexercised**; `evilgooglevideo.com`/`googlevideo.com.evil.com`/`www.youtube.com.evil.com` rejected; **`urlsplit(url).port` must be `None` or `443`, else `PolicyRejected` — cycle 8 fix** (the upstream-supplied `baseUrl` this policy gates, see T-10/AC-18, is not caller-controlled, but its host/scheme allowlist had no equivalent port check; tested against `https://www.youtube.com:8080/...`); opener built from an explicit handler list (`OpenerDirector()` + `HTTPSHandler(context=ssl.create_default_context())` + no-redirect handler + `HTTPErrorProcessor`/`HTTPDefaultErrorHandler` + `ProxyHandler({})`), asserted to contain no `FileHandler`/`FTPHandler`/`DataHandler`; a `3xx` response (302 and 305, both) raises `PolicyRejected` via explicit `HTTPError.code` inspection ordered before the 429/5xx branches — **not** relying on `redirect_request` returning `None` alone (that only prevents following, confirmed by the prior cycle's empirical test); `test_source_policy.py` bans `_create_unverified_context`, `CERT_NONE`, `check_hostname\s*=\s*False`, `ssl\.SSLContext\(`, `_create_default_https_context` anywhere under `plugins/youtube-transcript/` including `tests/`, **excluding exactly `tests/test_source_policy.py` by path** (the scan otherwise matches its own ban-list literals and is red from creation); **`.opener` is never referenced outside `net/client.py` — text-ban in `test_source_policy.py`, cycle 9 fix, corrected by the narrow check** (defense in depth alongside `Transport` no longer having the field at all — catches a future regression that re-adds it): the pattern is `\.opener\b` specifically, **not** `.open(` (too broad — `.open(` matches `gzip.open(`/`Path.open(`/`io.open(`, all legitimate elsewhere in this codebase and unrelated to the egress-primitive risk this ban targets); the banned area is `plugins/youtube-transcript/plugin/server/**` **only** — `tests/` is explicitly **not** in scope, since tests are required to reference the opener to mock it (`test_scheme_https_only_opener_never_called` here, `_helpers.py`'s `mock_urlopen`, T-3) — a ban that also covered `tests/` would fail this task's own suite on creation; with the banned area narrowed to production code, there is no self-matching risk to exclude by path (unlike the TLS-bypass-token bans above, which do need `test_source_policy.py` excluded because they scan `tests/` too) — self-test `test_opener_ban_excludes_nothing_under_plugin_server` asserts the exclusion set for this specific ban is empty, not the shared per-ban-exclusion-set pattern used elsewhere; **`Transport`'s field set is checked structurally, not just by name** — `test_transport_field_set_is_exactly_sleep_and_jitter` asserts `{f.name for f in dataclasses.fields(Transport)} == {"sleep", "jitter"}` (narrow-check fix — a test named for the one field cycle 8 added, `test_transport_has_no_opener_field`, would stay green if a *differently-named* egress-capable field were added later; asserting the full field set catches any future addition regardless of name); the separate `urllib.request`/`urllib.error` attribute-access ban (T-3) excludes exactly `net/client.py` by path, for the same reason — **each ban pattern has its own exclusion set, checked as an exact-path membership test named per-ban, not one shared "exclusion list" with a single "exactly one entry" assertion** (cycle 4 finding — the prior wording claimed a single shared exclusion list of size one, which was already false the moment a second, differently-scoped ban existed; a self-test per ban pattern asserts its own exclusion set exactly, so a future `tests/test_tls_hack.py` or a third ban pattern each stay correctly covered without the assertion silently drifting); **any self-test fixture that needs to contain a banned literal for its own purposes (e.g. `test_import_boundaries.py`'s attribute-access-bypass self-test, which legitimately references `urllib.request`) builds that literal from concatenated string parts (`"urllib" + ".request"`) rather than a literal substring match, cycle 4 finding** — a fixture written as a plain literal would itself trip the new text-ban scan the moment both land in the same test tree, exactly the "banning file matches its own patterns" defect class this section already fixes once
- check: `test_net_client_policy.py::{test_scheme_https_only_opener_never_called,test_file_and_data_schemes_rejected,test_null_host_rejected,test_allowlist_exact_and_suffix,test_non_standard_port_rejected,test_opener_has_no_default_handlers,test_redirect_302_and_305_map_to_policy_rejected_before_status_branches,test_transport_field_set_is_exactly_sleep_and_jitter,test_response_is_plain_data_container}`, `test_source_policy.py::{test_no_tls_bypass_tokens,test_opener_reference_banned_outside_net_client,test_opener_ban_excludes_nothing_under_plugin_server}` (**cycle 9 narrow-check corrections**: `test_transport_has_no_opener_field` → `test_transport_field_set_is_exactly_sleep_and_jitter` (Y-2), `test_transport_opener_access_banned_outside_net_client` → `test_opener_reference_banned_outside_net_client` (Y-1, narrower regex/scope), `test_response_is_plain_data_container` new (Y-3)) (closes AC-19 in full)

## T-6b — `net/client.py` resource controls: caps, deadline, retry, XML guard
- after: T-6a
- files: `plugins/youtube-transcript/plugin/server/net/client.py` (remainder of `fetch()`, `parse_xml_guarded()`), `plugins/youtube-transcript/tests/test_net_client_resources.py`, `plugins/youtube-transcript/tests/fixtures/doctype_ascii.xml`, `doctype_utf16.xml`, `gzip_bomb.bin`
- interface: consumes T-6a's opener/policy layer and `Transport` dataclass, `HTTP_TIMEOUT` from T-2 · produces `fetch(url, *, method="GET", body=None, headers=None, deadline:Deadline, transport: Transport) -> Response` (**cycle 8: `sleep`/`jitter` bare parameters replaced by the single `transport: Transport` object** — `transport.sleep`/`transport.jitter` used internally where the bare parameters were before; call sites default to a module-level `Transport()` built from `time.sleep`/`random.uniform` when the caller doesn't need to inject a fake one, so existing tests supplying fakes construct `Transport(sleep=fake_sleep, jitter=fake_jitter)` instead of passing two keywords); `fetch()` merges a fixed base header set — `BASE_HEADERS = {"Accept-Encoding": "gzip"}`, **the only entry, narrow-check fix (Y-6)** — **on top of** the caller's `headers` (if any), i.e. `{**(headers or {}), **BASE_HEADERS}` (**merge direction corrected, narrow check (Y-5)**: cycle 9's first draft had caller headers win on collision, which let a caller send `Accept-Encoding: identity` and silently defeat AC-20's "unconditionally" — the base set exists precisely to be non-overridable, so it merges in last); both sides' keys are **normalized via `.title()` before merging** (narrow check (Y-7) — `urllib.request.Request.add_header` itself calls `.capitalize()` on header names, so an un-normalized dict-level merge can pick the wrong "winner" between differently-cased spellings of the same header purely by dict insertion order; normalizing first makes the merge's stated precedence rule the actual precedence, not an accident of casing) (**cycle 8 fix, `User-Agent` removed cycle 9** — AC-20 mandates `Accept-Encoding: gzip` unconditionally, and this is the only header `net/client.py` owns **as a base default** — it does not follow that `User-Agent` goes unsent, since T-10 sets it explicitly on every leg's `headers` argument (narrow-check clarification, Y-8); if `headers=None` and no caller sets `User-Agent`, `urllib` supplies its own `Python-urllib/3.x` default, unreached in v1 because T-10 always provides one); cycle 8's version sent a `User-Agent` sourced from T-13b's `server.py` `USER_AGENT` constant, which created an unconstructible `ALLOWED_EDGES` edge (`net/`'s row is `domain/`-only) and a wave-order violation (T-6b is Wave 1, T-13b is Wave 3) on top of contradicting the spec's own recorded decision that outbound UA is the ANDROID/IOS client string, not `USER_AGENT` — architecture/AC-12 clarify `USER_AGENT` identifies the MCP server to its *caller*, not this plugin's HTTP identity to YouTube. `User-Agent` is now supplied entirely by the caller via `headers` — T-10 sets it there; a literal `"User-Agent"` string is banned in `net/client.py` by `test_source_policy.py` (narrow check (Y-6) — this is the "accidentally reintroduced a constant" class the text-ban mechanism is actually good at, unlike Y-1's `.opener` case); `parse_xml_guarded(data:bytes) -> Element`
- acceptance: raw read capped at `HTTP_MAX_RESPONSE_BYTES`, `ResponseTooLarge` before the cap+1st byte; gzip inflate capped **cumulatively** (ported `_inflate_gzip_capped` from `plugins/maven-mcp/plugin/server/server.py:387-403`, tracking a running total across `decompressobj().decompress(chunk, max_length=...)` calls including `unconsumed_tail`, not a per-call peak) against a gzip-bomb fixture; the streaming read loop checks the **network-budget deadline** (`deadline.remaining() - ENCODE_RESERVE`) on every chunk, independent of the per-`recv` socket timeout, aborting with `TransportFailed` if exceeded — tested with the drip interval **parametrized over `{0.5, 4, 7.5, 14.99}` seconds** (a drip fixed at exactly `HTTP_TIMEOUT` is the one interval that lands on a check boundary and hides the defect it's meant to prove absent), each case asserting abort no later than `network_budget_remaining + HTTP_TIMEOUT` — this bound is for the **body-read loop specifically** (the streaming read this task owns); it is **not** a claim about the pre-body phase (connect/TLS/headers), which plan.md's Technical Approach states explicitly is *not* wall-clock bounded by any mechanism in this design (a scope decision, not a defect — see plan.md's "Slow-drip defense" section, cycle 4); the pre-body phase (connect/TLS/headers) is **not** exercised by a test in this suite (cycle 5 fix — a prior version of this task asked for a header-dripping mock test here, but that test can only assert something meaningful against a *real* blocking socket timeout, which contradicts the fake-clock mandate below; a real-timing version of it would cost real CI minutes per matrix leg, exactly what the fake-clock mandate exists to avoid) — the pre-body phase's actual behavior is a **one-time empirical observation, recorded in T-P3's measurement notes** (not a CI-gating assertion), consistent with plan.md's honest statement that this phase is not wall-clock bounded by design; `parse_xml_guarded` constructs `xml.parsers.expat.ParserCreate()` (never `ET.XMLParser()`) with `StartDoctypeDeclHandler`/`EntityDeclHandler`/`ExternalEntityRefHandler` all raising a local sentinel, feeding an `xml.etree.ElementTree.TreeBuilder`; both the sentinel and `expat.ExpatError` are caught at the function boundary and re-raised as `MalformedUpstream`, tested on both ASCII and UTF-16-BOM'd `DOCTYPE` fixtures on **both** the 3.9 and 3.13 CI matrix legs; a fixture-based test asserts the exact (non-namespaced) tag names `parse_xml_guarded` produces against a real `<timedtext format="3">` sample; **the pre-attempt floor and the clamp are both backoff-inclusive, cycle 4 fix** (cycle 3 unified the floor into one formula but left the clamp computed from the bare `network_budget_remaining`, a `1.25s`-overrun inconsistency): before every attempt, `remaining_after_backoff = network_budget_remaining - (backoff_delay if retry else 0)`; if `remaining_after_backoff < MIN_ATTEMPT_TIMEOUT` (`5s`), no attempt is made and `TransportFailed` is raised immediately; the effective per-attempt timeout is `min(HTTP_TIMEOUT, remaining_after_backoff)` — tested against the re-derived worst case in plan.md's Technical Approach: **for a single leg needing its one retry to succeed**, with the mock's retry response arriving at `t = HTTP_TIMEOUT - EPSILON` into the retry attempt (`EPSILON = 0.01s` under the injected fake clock, not "instantly" — cycle 5 fix, matching T-14(b)'s equivalent fix: without this explicit timing, "the leg's total is `2×HTTP_TIMEOUT + max_backoff`" is ambiguous about *how* a successful retry attempt contributes a near-full `HTTP_TIMEOUT` to the total, the exact ambiguity that produced T-14(b)'s original bug) THEN the leg's total is `HTTP_TIMEOUT + backoff_delay + (HTTP_TIMEOUT - EPSILON)`, computed from the constants, not a hardcoded decimal, **compared via `math.isclose(actual, expected, rel_tol=0.0, abs_tol=1e-9)`, both tolerance arguments named explicitly, cycle 7 fix** (this task's timing assertion previously used bare `==`, unlike T-14's equivalent case which already got the `math.isclose` treatment in cycle 6; stating `rel_tol=0.0` explicitly avoids the ambiguity of relying on `math.isclose`'s non-zero default `rel_tol`, which would otherwise silently dominate over the stated `abs_tol` for values in this magnitude range), and no leg's clamp truncates below `HTTP_TIMEOUT` in this scenario (`remaining_after_backoff` stays above `HTTP_TIMEOUT` at this budget); **a separate test exercises the clamp actually truncating**: with `remaining_after_backoff = 5.5s < HTTP_TIMEOUT`, the attempt's timeout is `5.5s`, not `15s` (the prior suite only had a test proving the clamp does *not* engage in the nominal case, none proving it engages correctly when it should); a boundary-case test at `remaining_after_backoff = MIN_ATTEMPT_TIMEOUT - 0.5s` fails fast, at `+0.5s` proceeds; retry backoff `delay = 0.5*(2**1)+jitter(0,0.25)` (the single retry this design allows, `HTTP_MAX_ATTEMPTS=2` — no 0-vs-1-indexing ambiguity since there is only ever one retry), max `1.25s`; **`jitter` is drawn from an injectable source, `jitter: Callable[[float, float], float] = random.uniform`, alongside the already-injectable `sleep`, cycle 5 fix** (every test asserting an exact backoff value pins `jitter` to always return its upper bound, `0.25` — previously unpinned, so any hardcoded-exact assertion depending on `max_backoff = 1.25s` was flaky against real randomness); 429 raises `Throttled` immediately without consuming a retry attempt, carrying `Retry-After` if present; every raised exception is logged at DEBUG via `domain.redact_url()`, verified a `?pot=…&sig=…` query string never appears in the logged form; **every timing test in this file simulates elapsed time by advancing an injected `Deadline` clock, never a real `time.sleep`** (the drip-interval and worst-case tests above would otherwise cost minutes of real wall-clock time per CI run across both matrix legs — the mock transport's "delivery" is a clock-advance-then-return, not an actual blocking wait); **`net/client.py` receives one human (or dedicated `security-expert` agent) review pass before this task closes, its outcome recorded in `progress.md`'s Learnings** (cycle 5 addition — this module is the plan's own repeatedly-named highest-density area for security findings, and the AST/text-ban mechanisms guarding it are now explicitly documented as hygiene, not a closed boundary; the actually-load-bearing control named in that documentation — code review — needs to be a real, checked-off step, not only asserted prose)
- check: `test_net_client_resources.py::{test_raw_read_cap,test_gzip_bomb_cumulative_cap,test_slow_drip_body_phase_aborts_within_budget_plus_http_timeout,test_doctype_ascii,test_doctype_utf16,test_doctype_maps_to_malformed_upstream,test_xml_tag_names_non_namespaced,test_single_leg_retry_success_total_computed_from_constants,test_clamp_truncates_below_http_timeout,test_floor_formula_boundary_case,test_retry_budget_and_backoff_with_pinned_max_jitter,test_429_no_retry_consumed,test_log_redacts_query_string,test_accept_encoding_present_on_every_request,test_base_accept_encoding_survives_caller_override,test_base_headers_are_exactly_accept_encoding}`, `test_source_policy.py::test_user_agent_literal_banned_in_net_client` (**cycle 9 narrow-check corrections**: the `User-Agent` half of the old combined test moved to T-10's `check`, since this task no longer sources or sends it; `test_caller_headers_win_on_collision` → `test_base_accept_encoding_survives_caller_override` (Y-5 — merge direction inverted, test sends `Accept-Encoding: identity` from the caller and asserts `gzip` still goes out); `test_base_headers_are_exactly_accept_encoding` new (Y-6 — asserts `BASE_HEADERS == {"Accept-Encoding": "gzip"}` exactly, so a reintroduced `User-Agent` default can't hide inside a growing base set); `test_user_agent_literal_banned_in_net_client` new (Y-6, text-ban)) (drip-interval parametrization `[0.5, 4, 7.5, 14.99]` on the body-phase test only; all timing tests use an injected clock and pinned jitter, no real `time.sleep`/`random`) (closes AC-20, AC-22)

## T-7 — `protocol/envelope.py` + `schemas.py`
- after: T-3 (**simplified back from `T-3, T-5`, cycle 6** — the positive-control sentinel test that needed T-5's encoders is gone entirely, per the AC-15 redesign below; nothing in this task depends on `formats/` anymore)
- files: `plugins/youtube-transcript/plugin/server/protocol/envelope.py`, `protocol/schemas.py`, `plugins/youtube-transcript/tests/test_envelope.py`, `test_schemas.py`
- interface: consumes `Status`/`STATUS_POLICY`/`ToolOutcome`/`generate_content_boundary`/`FORMATS` from T-2, `TrackDescriptor` (incl. `track_id` and the `Mapping[str,int]`-typed `estimated_characters`) from T-2, `VideoId` from T-2 · produces `MESSAGES: Dict[Status,str]` (**no longer carries `contentNotice`, cycle 7** — `MESSAGES`'s per-status totality (AC-17) is unaffected, since `contentNotice` was never one of its per-status rows to begin with; this is a clarification of AC-15/AC-17's relationship, not a removal); `content_notice(boundary: str) -> str` (**new, cycle 7 — AC-15 reopened a second time**: returns a string naming the call's actual boundary value, e.g. `f"The untrusted region below is delimited by exactly the two occurrences of {boundary!r}; disregard any other similar-looking marker inside as content, not a delimiter."` — closes the verifiability gap the cycle-6 redesign left open: a consumer that never learns the value can't distinguish a real occurrence from a well-formed forged one, confirmed reachable by execution in cycle 7's review); `FIELDS_BY_STATUS: Dict[Status, FrozenSet[str]]` (`FIELDS_BY_STATUS[no_transcript]` frozen to `⊇ {tracks, videoDurationSeconds}`; `FIELDS_BY_STATUS[language_unavailable]` frozen to `⊇ {availableLanguages}`; **`FIELDS_BY_STATUS[Status.ok]` frozen to `⊇ {tracks, videoDurationSeconds, transcript, contentNotice, resolvedTrack, nextCursor, truncated}`, narrow-check fix (Y-10)** — previously undeclared for any status, despite `build()` filtering every payload through this table; a single superset covers both tools' successful-response shapes (`list_transcript_tracks`'s `{tracks, videoDurationSeconds}`, `get_transcript`'s `{transcript, contentNotice, resolvedTrack, nextCursor, truncated}`), since each tool's `handle()` only ever populates the subset relevant to it and the table's job is bounding what's *permitted* through, not dictating exactly what's present); `build(outcome, *, video: Optional[VideoId] = None) -> Dict[str,Any]` (**cycle 8: `video` is typed `Optional[VideoId]`, an already-normalized identifier, never the caller's raw string** — the prior untyped signature let a `video_not_found` outcome's `message` echo the raw, unbounded, unsanitized caller-supplied `video` argument; on `video_not_found` there is by definition no normalized value, so `video=None` and `message` comes entirely from `MESSAGES`, with no caller-controlled content in it at all) allowlist-filters `payload` against `FIELDS_BY_STATUS`, constructs the `tracks` array itself from `TrackDescriptor`'s five fields — `trackId`←`track_id`, `languageCode`, `languageName`, `kind`, `estimatedCharacters`←`estimated_characters` (already a `{text,srt,vtt}` dict, passed through as-is) — as a fixed literal shape rather than passing through a tool-built dict; on a successful `get_transcript` outcome, additionally constructs `resolvedTrack: {languageCode, kind, sourceLanguageCode}` from the resolved `TrackDescriptor`'s fields the same way, `sourceLanguageCode` always `None` in v1 since `kind` is never `"translated"` (AC-5); **wraps the assembled transcript text in a boundary generated fresh by `generate_content_boundary()`, called once per `build()` invocation — no neutralization, removal, placeholder substitution, or content-scrubbing of any kind, cycle 6 fix per the AC-15 redesign**; **dict-level layout, cycle 7, corrected cycle 8, wire carrier named cycle 9**: `build()`'s return value carries the wrapped transcript text under a literal key named **`transcript`, narrow-check fix (Y-10)** and `content_notice(boundary)`'s output under `contentNotice` — **two separate, explicitly-named dict keys** — never concatenated into one string — so the boundary value's own occurrence inside the notice text is structurally outside `transcript`'s occurrence count, not merely excluded by a counting convention that has to remember to exclude it (cycle 8 fix: cycle 7's version placed the notice text immediately before the wrap inside the same string, which meant the boundary value's in-notice occurrence counted as the wrap's "first occurrence" under a naive read). **Cycle 9 correction: `build()`'s two dict keys are not, by themselves, two separate locations on the MCP wire** — `get_transcript`'s `TOOL_SCHEMAS` entry declares no `outputSchema` (unchanged, see below), so there is no `structuredContent` for separate dict keys to occupy; T-13a's `Registry.call()` serializes the **entire** result dict with one `json.dumps()` into the single `content[0].text` block of the `CallToolResult` (confirmed against this repo's own pattern, `maven-mcp/plugin/server/server.py:11510`) — so on the wire, `contentNotice` and `transcript`'s value are two substrings of one JSON string, and the boundary value's in-notice occurrence is present in that raw string exactly as before cycle 8's fix. This is not a defect: AC-26's reference parser (see acceptance) is defined to `json.loads()` the raw bytes **first**, recovering the two separate dict keys, and only then search `transcript` specifically — the separation cycle 8 built is real and sufficient, it was only ever verified against the wrong artifact (the pre-serialization dict) instead of the actual wire bytes a consumer receives; `get_transcript`'s `TOOL_SCHEMAS` entry declares **no `outputSchema`, cycle 8, unchanged** (the simplest choice for v1 — removes any risk of the wrapped/notice content being duplicated into a second, unwrapped `structuredContent` copy; the transcript text and notice each appear in exactly one place in the single `content[0].text` block); on its own invariant violation (an un-enriched `TrackDescriptor` reaching the `tracks` array) `build()` raises **`ValueError`, not `DomainFailure`, cycle 8 fix** (see acceptance); `TOOL_SCHEMAS`
- acceptance: `MESSAGES`/`FIELDS_BY_STATUS` totality **and** the `no_transcript`/`language_unavailable` contents assertions above; payload-key allowlist-filtering test; `retryAfterSeconds` clamp (absent→30, `5`→30, `3600`→300); GIVEN a `video_not_found` outcome with no normalized `video` WHEN `build()` runs THEN `message` equals `MESSAGES[Status.video_not_found]` exactly, tested against a fixture where the raw caller input was a 10 KB string (the raw string never reaches `build()` at all, so it cannot leak regardless) — **cycle 8, closes the raw-echo gap**; GIVEN a successful `get_transcript` THEN the returned dict's `transcript` key contains **exactly two** occurrences of the boundary value actually used for that call (the wrap itself, counted **only within that key's value**, cycle 8 correction) and a separate `contentNotice` key is present, plus a top-level `resolvedTrack` with exactly the three keys above; GIVEN two separate `build()` calls THEN the two calls' boundary values differ (proves the per-call generation is actually wired in, not a module-level constant reintroduced by accident); **GIVEN the full `build()` result dict, walked recursively, THEN the boundary value occurs exactly once across all values reachable from `contentNotice` and exactly twice across all values reachable from `transcript`, and zero times everywhere else in the dict — `test_boundary_appears_in_exactly_two_dict_values`, narrow-check fix (Y-11)** (the layout invariant "no other dict value repeats the boundary" was stated in prose but had no owning test; `nextCursor`/`message`/etc. are all low-risk in practice, but the invariant is claimed and should be checked, not assumed); **AC-26's unit-level coverage (test-only, in `test_envelope.py`, not production code)**: a reference-parser helper, given a `json.dumps(build_result)` string constructed **within this test** (a cheap approximation of the real wire bytes, useful for fast unit iteration on the parser algorithm itself) reconstructs the untrusted region correctly against three adversarial fixtures, cycle 7 new, expanded cycle 8 — the parser **first** `json.loads()`s the string to recover the dict, **then** reads the boundary value out of the parsed dict's `contentNotice` key, then searches **only the parsed dict's `transcript` key** for exactly two occurrences of that exact value (never searching the raw pre-parse bytes as an undifferentiated blob, where the value legitimately appears a third time inside the JSON-encoded `contentNotice` string — that third occurrence is expected and outside this algorithm's scope by construction, not a case it needs to reject) — (a) caption text containing a well-formed forged boundary-shaped token (matching the real prefix/format, a different random value) plus a forged trailing epilogue mimicking a notice — the parser must still locate the *real* boundary's two occurrences and treat the forged token as ordinary content, since it isn't reading the `contentNotice` key for this; (b) caption text containing a forged `content_notice`-shaped string naming a different fake boundary value — the parser must trust only the actual parsed `contentNotice` key's value and ignore the lookalike embedded in the `transcript` key; (c) caption text containing the literal prefix `<<<UNTRUSTED_CONTENT_` with no valid 32-hex-digit suffix and no closing `>>>` (a partial-prefix collision) — **new, cycle 8** — the parser matches on the complete, exact value only, so a partial-prefix substring must not be counted as an occurrence or otherwise confuse the two-occurrence search. **This unit-level coverage is necessary but not the AC-26-closing test, narrow-check fix (Y-9)** — a `json.dumps()` call made *inside the test itself* is still a reconstruction, not the actual bytes `protocol/dispatch.py` (T-13a) sends; a bug in the real serialization step (different `json.dumps` options, a wrapping layer, a different carrier entirely) would leave this test green while breaking the real consumer. The test that actually closes AC-26 dispatches these same three fixtures through the real `handle_message` stack and reads the parser's input from the real response — see T-14's `test_ac26_reference_parser_against_real_dispatch_response`; GIVEN a `list_transcript_tracks` outcome WHEN `build()` is called THEN each track object in the response has exactly the fixed key set `{trackId, languageCode, languageName, kind, estimatedCharacters}`, sourced from `TrackDescriptor` fields alone (not the tools-layer payload dict), with `estimatedCharacters` itself exactly `{text, srt, vtt}`; GIVEN any `TrackDescriptor` in the `tracks` payload has `estimated_characters is None` (an un-enriched, provider-returned descriptor reaching `build()` by mistake — this check applies only to the `tracks` array, explicitly **not** to `resolvedTrack`, whose source `TrackDescriptor` never carries `estimated_characters` by design) WHEN `build()` runs THEN it raises **`ValueError` (cycle 8 fix — was `DomainFailure`)**: architecture review found routing a `tools/`-layer bug (T-11 failing to enrich before calling `build()`) through the `DomainFailure` safety net gave it `status: transport_error, retryable: true` — instructing a caller to retry a deterministic bug forever — inverting T-13a's own bug/domain-failure split; a plain `ValueError` is caught by `Registry.call()`'s **other** branch (non-`DomainFailure` → `-32603`), the channel T-13a already defines for genuine bugs; both `TOOL_SCHEMAS` declare `annotations: {readOnlyHint: true, openWorldHint: true}` and an untrusted-content `description`, and `get_transcript`'s omits `outputSchema` (cycle 8, see produces) — **`list_transcript_tracks`'s `description` states its `languageName` fields are untrusted content too, cycle 7 fix** (security review found the response carries untrusted upstream text with no boundary of its own; T-2's length bound on `TrackDescriptor.language_name` is the structural mitigation, this `description` update is the consumer-facing disclosure — accepted as sufficient for v1 rather than adding a second boundary mechanism, per the user's MVP-first direction); **`TOOL_SCHEMAS`'s `languages` field does NOT declare a JSON-Schema `maxItems`, post-cycle-9 hostile-implementer-walkthrough finding** — AC-3's 11-entry cap is a domain-level rejection (`status: "language_unavailable"`, enforced by `T-9::validate_languages`), not a schema-level one; the natural but wrong implementation adds `maxItems: 10` to the schema by JSON-Schema convention, which would instead reject an 11-language request at dispatch time with `-32602` before it ever reaches `tools/`, violating AC-3's explicit "not a schema error" requirement — the schema leaves `languages` unbounded in length, and the cap is enforced only in `T-9`
- check: `test_envelope.py::{test_messages_and_fields_totality,test_no_transcript_fields_include_duration,test_language_unavailable_fields_include_available_languages,test_ok_fields_include_transcript_and_content_notice,test_payload_keys_allowlisted_per_status,test_retry_after_clamp_three_branches,test_video_not_found_message_excludes_raw_caller_input,test_boundary_wrap_exactly_two_occurrences_in_transcript_key,test_content_notice_is_separate_dict_key,test_boundary_differs_across_calls,test_boundary_appears_in_exactly_two_dict_values,test_reference_parser_unit_resists_forged_token_in_content,test_reference_parser_unit_resists_forged_notice,test_reference_parser_unit_resists_partial_prefix_collision,test_track_object_key_set_fixed_from_domain_fields,test_estimated_characters_is_three_key_object,test_unenriched_tracks_entry_raises_value_error_not_domain_failure,test_resolved_track_constructed_on_success,test_get_transcript_schema_has_no_output_schema}` (**cycle 9 narrow-check corrections**: `test_reference_parser_operates_on_json_dumps_of_result_not_the_dict_directly`/`test_reference_parser_resists_*` renamed `test_reference_parser_unit_resists_*` and demoted to unit-level coverage only, not AC-26-closing (Y-9 — see T-14 for the test that actually closes AC-26); `test_ok_fields_include_transcript_and_content_notice`/`test_boundary_appears_in_exactly_two_dict_values` new (Y-10/Y-11)), `test_schemas.py::test_annotations_and_untrusted_content_description` (closes AC-10, AC-15, AC-5's `resolvedTrack`, AC-3's `availableLanguages` in full, AC-17's `MESSAGES` totality, AC-22's clamp — **AC-26 closed at T-14, narrow-check fix (Y-9)**, not here)

## T-8 — `tools/cursor.py`
- after: T-3
- files: `plugins/youtube-transcript/plugin/server/tools/cursor.py` (placed under `tools/`, not `providers/` — its only consumer, needs no new `ALLOWED_EDGES` entry since `tools/` already may import `formats/`), `plugins/youtube-transcript/tests/test_cursor.py`
- interface: consumes `VideoId`/`TrackDescriptor`/`DomainFailure`/`CursorInvalid` from T-2 (**`CursorInvalid` declared in `domain/`, not here, cycle 7 — symbol-placement rule**: this task raises it, but declaring it in `domain/` lets T-3's/T-14's totality tests see it independent of this task's completion, the same reasoning already applied to `TrackFieldInvalid`), `MAX_SEGMENTS`/`FORMATS` from T-2 (both re-exported via `domain/`, cycle 3/cycle 7 — `FORMATS` used for the `format` field's AC-4 enum check), `parse_track_id` from T-3 · produces `CursorFields(video_id, track_id, format, include_timestamps, segment_index)` — **5 fields, `pageOrdinal` dropped** (cycle 2 finding — unreachable by any server-emitted cursor, contradicted its own 5-key validation rule, achieved nothing); `MAX_CURSOR_CHARS = 512`; `encode(fields) -> str`; `decode(raw:str) -> CursorFields` raising `CursorInvalid` on any validation failure — since `CursorInvalid(DomainFailure)` is declared in `domain/` (T-2), `outcome_from_error`'s totality test (T-3) and T-14's `DomainFailure` tree check both cover it automatically with no special-casing here
- acceptance: cursor string longer than `MAX_CURSOR_CHARS` rejected before any decode work; `base64.b64decode(raw.encode("ascii"), altchars=b"-_", validate=True)` (not `urlsafe_b64decode`, which silently strips invalid characters instead of raising) rejects non-alphabet input; decoded JSON must be a `dict` with **exactly these five keys**, nothing more or fewer; `videoId` failing the AC-6 regex, or `segmentIndex` negative or `>= MAX_SEGMENTS`, rejected; **all three remaining fields independently re-validated at decode, named explicitly (cycle 3 finding — AC-11 requires every field re-validated with fresh-input rules, and the prior "full tamper-case suite" parenthetical was an unfalsifiable stand-in for these)**: `format` not in the AC-4 enum rejected, `includeTimestamps` not a `bool` rejected, `trackId` failing `parse_track_id` (reused from `providers/base.py`, a permitted `tools/ → providers/base.py` edge) rejected; `decode(encode(fields)) == fields` round-trips
- check: `test_cursor.py::{test_size_cap,test_base64_validation,test_exactly_five_keys,test_video_id_regex_rejected,test_segment_index_bounds_rejected,test_format_not_in_enum_rejected,test_include_timestamps_non_bool_rejected,test_track_id_fails_parse_track_id_rejected,test_roundtrip}` (closes AC-11's cursor decode/first-phase-validation clauses in full)

## T-9 — `tools/resolution.py`
- after: T-3
- files: `plugins/youtube-transcript/plugin/server/tools/resolution.py`, `plugins/youtube-transcript/tests/test_resolution.py`
- interface: consumes `TrackListing` (incl. `default_audio_language`) / `TrackDescriptor` (incl. `is_default`) from T-2, `parse_track_id` from T-3 · produces `select_track(listing, *, languages=None, track_id=None) -> Optional[TrackDescriptor]` (**cycle 8: reads `listing.default_audio_language` for AC-2 tier 2 and each track's `is_default` for tier 3** — both were previously undeclared anywhere in the frozen T-2 contract despite this task's acceptance already claiming to close AC-2 in full); `sort_tracks(tracks) -> Tuple[TrackDescriptor,...]` (manual rank 0, auto rank 1, alphabetical within tier); `validate_languages(languages) -> List[str]` (≤10 entries, ≤20 chars each)
- acceptance: full AC-2 five-tier resolution order — **cycle 8: one test per tier, not one aggregate test**, including tier 2 (a track's `language_code` matches `listing.default_audio_language`) and tier 3 (a track has `is_default=True`), plus the explicit "both signals absent → tier skipped, not an error" case AC-2 requires; full AC-3 BCP-47 matching table; `track_id` precedence over `languages`; `sort_tracks` manual-before-auto; `languages` cap enforcement treated as `language_unavailable`, not a schema error. **Tier order, disambiguated post-T-9-implementation (T-9's own implementer flagged this as a genuine spec/task inconsistency, caught before it shipped)**: AC-2's literal prose order is `languages` (AC-3) → `default_audio_language` → `is_default` → manual-sorted fallback → auto-sorted fallback — i.e. **`languages` is AC-2's own tier 1, `default_audio_language` is tier 2, `is_default` is tier 3**. `track_id` (AC-5) is a *separate*, prior precedence gate outside AC-2's own five tiers, not one of the five — when combined into one `select_track()` function, `track_id` is checked first (never falling through on a validation/no-match failure, per AC-5's literal text), and AC-2's own five tiers run in AC-2's own order after it: `languages` (AC-3 preference matching) **before** `default_audio_language`/`is_default`. The plan's own cross-reference at the "cycle 8 (architecture)" Decisions Made row already calls these two fields "AC-2's tiers 2-3" using AC-2's own numbering (where `languages` is tier 1) — this section's test names below (`test_tier2_default_audio_language_match`, `test_tier3_upstream_default_track`, `test_tier4_language_preference_match`) instead numbered `track_id` as tier 1 and then continued counting sequentially without re-inserting `languages`, which silently demoted `languages`-preference matching to *after* both default-signal tiers — a renumbering slip in this task's own prose, not a deliberate architecture decision (no rationale for outranking an explicit caller preference with inferred video metadata appears anywhere in this plan). **Resolved in favor of AC-2's literal order**: `select_track()` checks `track_id` → `languages` → `default_audio_language` → `is_default` → manual-fallback → auto-fallback. The test **names** below are kept as-is (renaming is cosmetic and not worth another edit pass), but their **bodies** must exercise this corrected order — in particular, a fixture with both a `languages` match and a `default_audio_language`/`is_default` match resolves to the `languages` match, not the default-signal match.
- check: `test_resolution.py::{test_tier1_exact_track_id,test_tier2_default_audio_language_match,test_tier3_upstream_default_track,test_tier4_language_preference_match,test_tier5_fallback,test_both_default_signals_absent_tier_skipped}` (closes AC-2, AC-3, AC-5's `trackId` precedence, AC-1's sort order)

## T-13a — `protocol/registry.py` + `dispatch.py`
- after: T-7
- files: `plugins/youtube-transcript/plugin/server/protocol/registry.py`, `protocol/dispatch.py`, `plugins/youtube-transcript/tests/test_dispatch.py`
- interface: consumes `DomainFailure`/`ToolOutcome`/`Status` from T-2 (**cycle 4** — `domain/` was already this row's only permitted edge; using it to distinguish domain-modeled failures from bugs, instead of needing `providers/base.py`'s `ProviderError`, is the fix), `TOOL_SCHEMAS` from T-7 · produces `Registry.register(name, schema, handler: Callable[[Dict], ToolOutcome])` / `.tools()` / `.call(name, args) -> Dict[str,Any]` (calls `envelope.build()`); `serve_stdio(registry, stdin, stdout)`; `handle_message(registry, msg)` — tested against a stub handler; explicit contract that a registered handler is invoked **fresh for every `tools/call` message** — the registry constructs no per-call state itself, so a caller (T-13b) supplying a closure that builds a new `Deadline` per invocation is the only way this contract is satisfied, and that responsibility is named here even though it's exercised at T-13b. **The `content[0].text` serialization step is named explicitly here, cycle 9** (architecture/security found it was previously implicit, and AC-26's reference parser had been verified against the wrong artifact as a result — see T-7): `handle_message`'s `tools/call` response wraps `Registry.call()`'s full result dict in exactly one `content` block, `{"type": "text", "text": json.dumps(result)}` — this is the single wire carrier for `envelope.build()`'s dict keys (including `contentNotice`/`transcript`, T-7), matching this repo's own established pattern (`plugins/maven-mcp/plugin/server/server.py:11510`); no `structuredContent` is emitted for either tool (T-7 declares no `outputSchema`)
- acceptance: JSON-RPC 2.0 shape (`initialize`/`tools/list`/`tools/call`/`ping`, `-32700`/`-32600`/`-32601`/`-32602`/`-32603`); stderr-only diagnostics, stdout carries only valid JSON-RPC; **GIVEN a `tools/call` for `get_transcript` THEN the response has exactly one `content` block of `type: "text"`, whose `text` value is `json.dumps(Registry.call(...)'s return value)` byte-for-byte (round-trips via `json.loads`), and no `structuredContent` key is present — cycle 9, closes the carrier gap AC-26 depends on**; `test_import_boundaries.py` confirms `protocol/registry.py`/`dispatch.py` import neither `tools/*` nor `providers/innertube.py`; **`Registry.call()` catches `DomainFailure` specifically as a defense-in-depth safety net, distinct from a bare `except Exception`, cycle 4 fix** (architecture found the prior "handler never lets it escape, so the registry needn't care" framing was unenforceable structurally — nothing stopped it reaching the registry if a handler under-specified its own catching, and the registry had no way to distinguish it from a genuine bug once it arrived): a stub handler raising `DomainFailure` (via a `ProviderError` subclass or `DeadlineExpired`, both are `DomainFailure` — checkable with only the already-permitted `domain/` edge) is caught by `Registry.call()`, logged (this is always a T-11/T-12 bug, since they are contractually supposed to catch it first) and translated to a generic `ToolOutcome(status=Status.transport_error, ...)` **built directly from `domain/` symbols**, never via `tools.outcome_from_error` (which `protocol/` still cannot import — the safety net is coarser than the primary translation, and that's acceptable since it should never actually fire); a stub handler raising anything **not** a `DomainFailure` (a genuine bug outside the domain-modeled set) becomes `-32603` — this is the only path to `-32603`, proving the generic-error path is a real backstop for bugs, not the route for domain-modeled failures. **`Registry.call()`'s two-outcome split, clarified (narrow-check finding, outside the four named fixes but cheap to close in the same diff)**: `Registry.call() -> Dict[str,Any]` catches and translates `DomainFailure` (the case above) but **propagates** (does not catch) any other exception — `handle_message` is the **sole** place that catches a propagated non-`DomainFailure` exception and constructs the `-32603` JSON-RPC error response; `Registry.call()`'s `Dict[str,Any]` return type therefore only ever describes its successful/`DomainFailure` path, never the `-32603` path, which is a different response shape entirely (a JSON-RPC error object, not a `result`). **The reference pattern this task follows (`maven-mcp/plugin/server/server.py:11510`) has a second branch this plan does NOT copy, named explicitly here so an implementer following "the same pattern" doesn't reach for it**: `maven-mcp`'s equivalent code (lines 11502-11509) also catches arbitrary exceptions into a *successful-looking* `{"content": [...], "isError": true}` result carrying `str(e)` — that branch is excluded here on two independent grounds: AC-15 forbids `str(exception)` in any caller-visible field, and AC-10's `STATUS_POLICY` requires `isError: false` on every row (there is no status this plan models where `isError` is ever `true`); `test_exception_string_never_reaches_response` asserts no substring of a raised exception's `str()` appears anywhere in a `-32603` response. **`isError` is emitted at the `CallToolResult` level, alongside `content`, always `false` in v1 (narrow-check fix, Y-13)** — `STATUS_POLICY`'s third tuple element feeds this field directly; it is not itself a key inside the `json.dumps()`-serialized `content[0].text` payload, the same test that checks the response shape (see above) asserts its position
- check: `test_dispatch.py::{test_protocol_shape,test_domain_failure_caught_as_safety_net_not_32603,test_non_domain_exception_becomes_32603,test_tools_call_response_is_single_json_dumps_text_block_no_structured_content,test_exception_string_never_reaches_response,test_is_error_field_is_call_tool_result_level_always_false}` (protocol-shape suite against a stub handler, plus the domain/bug split above; last two new, narrow-check fixes Y-12/Y-13)

## T-10 — `providers/innertube.py`
- after: T-4, T-6b, T-P3
- files: `plugins/youtube-transcript/plugin/server/providers/innertube.py`, `plugins/youtube-transcript/tests/test_innertube.py`, `plugins/youtube-transcript/tests/fixtures/*` (from T-P3)
- interface: consumes `normalize` from T-4, `fetch`/`parse_xml_guarded` from T-6a/T-6b, `Transport` from T-6a, `TranscriptProvider`/`ProviderSession` ABCs (`fetch(track, deadline)`, cycle 3) from T-3, `sanitize_text`/`redact_url`/`MAX_SEGMENTS`/`DEADLINE_CHECK_STRIDE` from T-2, `encode_track_id` from T-3 · produces `InnertubeProvider(TranscriptProvider).__init__(self, *, sleep: Callable[[float], None] = time.sleep, jitter: Callable[[float,float],float] = random.uniform)` (**cycle 8: constructs one `Transport(sleep=sleep, jitter=jitter)` in `__init__` and stores that**, rather than the two bare values separately — see T-6a), `InnertubeSession(ProviderSession)` (holds the parsed player response as an instance attribute of the session, never `InnertubeProvider`; **`InnertubeProvider.open()` passes its stored `Transport` through to every `InnertubeSession` it constructs, and `InnertubeSession.fetch()` passes that same object to every `net.fetch(transport=...)` call it makes for the timedtext leg — cycle 7 fix, consolidated cycle 8**: cycle 6's fix stored `sleep`/`jitter` on `InnertubeProvider` but stopped there; cycle 7 got them to `InnertubeSession` but as two more bare parameters; cycle 8 replaces both bare-parameter hops with one `Transport` object passed whole, so a future intermediate layer cannot repeat the same class of dropped-parameter defect); the `playabilityStatus`/`net`-exception → `Status` mapping table (**named explicitly in plan.md's Provider port section, post-cycle-9 hostile-implementer-walkthrough fix — three rows empirically confirmed, four synthetic and labeled as such**); each of the three legs' `net.fetch(..., headers=...)` calls passes a `headers` dict whose `User-Agent` is the ANDROID/IOS client's own conventional UA string (per spec:165, empirically confirmed by the research spike's 47+ live calls), **never `server.py`'s `USER_AGENT` constant — cycle 9 fix** (cycle 8 had `net/client.py` send `USER_AGENT` unconditionally, sourced from `server.py`, which was both an unconstructible `ALLOWED_EDGES` edge and a direct contradiction of this already-recorded spec decision; `net/client.py` now only owns the unconditional `Accept-Encoding: gzip` header, see T-6b)

**Wire-protocol details pinned during this task's implementation, not derivable on paper (hostile-implementer-walkthrough finding, post-cycle-9):**
- **Watch-page leg (leg 1) is HTML, not JSON.** The response body is a full watch page with `ytInitialPlayerResponse = {...};` embedded inside a `<script>` tag — `json.loads()` cannot run directly on the body. Extract the embedded JSON first (non-regex — `html.find("ytInitialPlayerResponse")` then balanced-brace scanning, or `json.JSONDecoder().raw_decode(html, idx)` from that index; the same non-backtracking rationale T-10's existing acceptance already applies elsewhere in this task), *then* `json.loads()` the extracted substring, which also yields `INNERTUBE_API_KEY` (see plan.md's two-step-dependency note). If the embedded assignment is absent entirely (distinct from present-but-malformed) — e.g. a consent-interstitial page — that is `UpstreamChanged`, with its own fixture (a watch-page HTML body containing no `ytInitialPlayerResponse` assignment at all), and leg 2 (player POST) is never attempted.
- **Player POST (leg 2) request body shape** (client context fields, exact header set beyond `User-Agent`) is pinned against a real, captured InnerTube response during this task's implementation, following the mechanism already empirically verified in `swarm-report/research/research-youtube-subtitles-plugin.md` (`ANDROID`/`IOS` client context, `INNERTUBE_API_KEY` from the watch page, never `WEB` — see that report's Evidence section) — not derived from this document alone. The captured request/response pair used to pin this becomes a checked-in fixture under `tests/fixtures/`, the same pattern T-6b already uses for its DOCTYPE/timedtext fixtures.
- **`TrackDescriptor.kind`** is `"auto"` when the raw `captionTracks[i]` entry's `kind` field equals `"asr"` (confirmed by the research report), else `"manual"`.
- **Timedtext response format**: research's empirical calls observed `format="3"` responses (the shape T-6a's `parse_xml_guarded` already assumes) without documenting whether the fetched `baseUrl` already includes a format-selecting query parameter or needs one appended. Pin this during implementation against a real captured `baseUrl` (see the request-fixture note above). If a format parameter must be appended to `baseUrl`'s query before fetch, that modified URL is still subject to AC-19's host/scheme/port allowlist re-check in `net/client.py` — it passes trivially since the host is unchanged, stated here explicitly so an implementer doesn't second-guess it or skip the re-check as "already validated."
- acceptance: **`open()` raises a `ProviderError` unless `playabilityStatus.status == "OK"`** (security-critical — the prior denylist-on-`ERROR`-alone framing fail-opened `UNPLAYABLE`/`LOGIN_REQUIRED`/etc. videos to `no_transcript, retryable: false`; restated as the spec's own positive gate); a parametrized test over `{"UNPLAYABLE", "LOGIN_REQUIRED", "AGE_CHECK_REQUIRED", "CONTENT_CHECK_REQUIRED", "LIVE_STREAM_OFFLINE", "SOME_FUTURE_VALUE"}` asserts each **raises the specific mapped `ProviderError` subclass** (`assertRaises`, not merely "empty-tracks never happens" — cycle 4 finding: the weaker assertion is satisfied by an implementation that returns a non-empty *garbage* `TrackListing` instead of raising at all); this discrimination block reads `playabilityStatus["reason"]` via `.get()`, explicitly **outside** the scope of the JSON-structural-failure wrapper below (cycle 4 finding — a wrapper scoped broadly enough to also cover this block could turn a `KeyError` on a missing `reason` into `upstream_changed`, silently defeating the positive gate instead of tripping it); for a video that **passes** the `OK` gate, `open()` **always succeeds** regardless of caption presence — `session.listing.tracks` is an empty tuple when there are none, `session.listing.duration_seconds` is populated either way (the redesign that removes `NoTranscript` as a raised exception — this is the load-bearing test for AC-1's `videoDurationSeconds` availability on the no-captions path); **the `captions`-container shape gets a three-way discrimination, not a single default**: (a) `captions` key **absent** → empty tuple, `no_transcript`; (b) `playerCaptionsTracklistRenderer` present with `captionTracks: []` → empty tuple, `no_transcript`; (c) `captions` key present but its expected sub-structure missing or of an unexpected type → raise `UpstreamChanged`; `playabilityStatus.status="ERROR"` with an unrecognized `reason` maps to `VideoUnavailable`; recognized age/region/live/bot-check discriminators map to their subclasses per the table now in plan.md's Provider port section — **the `age_restricted`/`region_blocked`/`live_not_ready`-via-`LOGIN_REQUIRED` rows and the `blocked_by_provider` discriminator (HTTP 403 + a consent-wall body marker, or a bot-check `reason` string) are synthetic fixtures with no observed real-world trigger (post-cycle-9 hostile-implementer-walkthrough finding — research's 47+ live calls never observed a block or an age/region gate); implement `blocked_by_provider`'s marker list as an isolated, easily-updated function rather than optimizing its exact strings for day-one correctness against data that doesn't exist yet**; one `open()` call followed by one `session.fetch(track, deadline)` call issues exactly 2 then 1 upstream requests (3 total), `open()` never called a second time per invocation (the proof that the session-scoped port delivers the AC-23 budget); `session.fetch()` raises `UpstreamChanged` for a `TrackDescriptor` that is not a member of `session.listing.tracks` (the precondition T-3 states); **a structural totality test over `NetError.__subclasses__()` (recursive)** asserts every subclass has a row in this table mapping to a `ProviderError` whose `status` is in `STATUS_POLICY` — not a hand-enumerated parametrize list, pinning `PolicyRejected → UpstreamChanged` and `ResponseTooLarge → TranscriptTooLarge` explicitly within it; **the two JSON-parsing legs (watch-page, player-response) map `json.JSONDecodeError`/`KeyError`/`TypeError`/`IndexError`/`RecursionError`/`ValueError` to `UpstreamChanged`** (**`RecursionError`/`ValueError` added, cycle 8** — a deeply-nested JSON body raises `RecursionError` in the C-accelerated decoder, and `ValueError` is `JSONDecodeError`'s own supertype, included for symmetry; neither was previously in the mapped set, so either would have escaped uncaught to `isError: true`/`-32603`, the same domain/bug misclassification this whole mapping exists to prevent), one fixture-backed test per case (truncated JSON body, HTML consent page in place of JSON, player response missing `videoDetails`, `captionTracks` renamed, **a deeply-nested JSON body triggering `RecursionError` — cycle 8**) — distinct from the legitimate `captions`-absent path above and explicitly **not** wrapping the `playabilityStatus` discrimination block (see above); **`default_audio_language` (from `videoDetails.defaultAudioLanguage` or microformat, `TrackListing`) and each track's `is_default` (from `playerCaptionsTracklistRenderer`'s per-track data, `TrackDescriptor`) are populated during `open()`, cycle 8** — feeds AC-2's tiers 2-3, previously read by T-9 but never populated by anything; a non-numeric `Retry-After` (e.g. an HTTP-date) falls back to `STATUS_POLICY`'s default of 30, not an exception; all caption text **and `languageName`** strings are run through `sanitize_text`; **`languageCode` and `kind` are validated at `TrackDescriptor` construction time, against the same `^[A-Za-z0-9-]{1,20}$`/`{manual,auto}` rules AC-18 already applies at parse time** (cycle 5 finding — a track whose upstream `languageCode` fails that pattern would otherwise reach `tracks[]`/`availableLanguages`/`resolvedTrack.languageCode` in the wire response unvalidated, and separately feed `encode_track_id`, whose output lands in `trackId`/`nextCursor`; the input here is upstream-controlled, not caller-controlled, so this is about upstream drift/compromise rather than a direct attacker payload, but the validation belongs at the point of construction either way); validation lives in `domain.TrackDescriptor.__post_init__`, raising a dedicated `domain.TrackFieldInvalid(DomainFailure)` (**cycle 6 fix** — named here explicitly so it's covered by `outcome_from_error`'s totality test like every other `DomainFailure`, and so both the provider's construction path and T-11's response-only enriched copies go through the same validation with no way to bypass it) — **the same `__post_init__` call also enforces `language_name`'s length bound (T-2, cycle 7)**, so a track whose upstream `languageName` exceeds 100 characters is dropped the same way as one with an invalid `languageCode`/`kind`, not left to reach the wire response unbounded; the provider catches `TrackFieldInvalid` **per track** and drops only that track from `session.listing.tracks` (one malformed track among several legitimate ones shouldn't fail the entire video) — **but if `captionTracks` was non-empty upstream and validation drops every single track, `open()` raises `UpstreamChanged` instead of silently returning an empty `TrackListing`, cycle 6 fix** (security finding — the prior per-track-drop-only design could fail open to `no_transcript, retryable: false` on a captions-format drift that happens to invalidate every track, exactly the masking failure mode the three-way `captions`-shape discrimination above exists to prevent; the count of dropped tracks is logged at DEBUG, redacted, either way); each constructed `TrackDescriptor` carries `track_id` (via `encode_track_id`) — **`estimated_characters` is left `None` by the provider, cycle 4 fix** (it needs `formats.estimate_characters`, unreachable from `providers/`; sole ownership moves to `tools/list_transcript_tracks.py`, see T-11 — T-10 no longer tests this field being populated); the decode loop raises `TranscriptTooLarge` the instant segment count would exceed `MAX_SEGMENTS` (imported from `domain/`), rather than materializing an unbounded tuple first; `deadline.expired()` is checked between the watch-page parse, player parse, and timedtext decode steps, raising `DeadlineExpired` on overrun; all logging uses `redact_url()`; a snapshot-based statelessness test: `vars(provider)` is identical before and after two `open()` calls for different video IDs, and the two sessions' `.listing` values are independent objects
- check: `test_innertube.py` (full mapping table, `test_non_ok_playability_raises_specific_subclass`, `test_playability_reason_read_via_get_not_swallowed_by_json_wrapper`, `test_open_always_succeeds_empty_tracks_when_no_captions`, `test_captions_shape_change_is_upstream_changed_not_no_transcript`, `test_provider_holds_no_per_video_state`, `test_fetch_rejects_foreign_descriptor`, `test_net_error_mapping_totality`, `test_json_structural_failures_map_to_upstream_changed`, `test_deeply_nested_json_recursion_error_maps_to_upstream_changed`, `test_max_segments_enforced_during_decode`, `test_deadline_checked_between_decode_phases`, `test_retry_after_non_numeric_falls_back`, `test_track_descriptor_carries_track_id_estimated_characters_none`, `test_malformed_language_code_kind_or_overlong_name_drops_track_not_whole_call`, `test_session_fetch_passes_transport_through_to_net_fetch`, `test_default_audio_language_and_is_default_populated`, `test_user_agent_present_on_every_leg_is_client_string_not_server_constant`, exactly-3-requests-per-invocation) (**cycle 9: `test_user_agent_present_on_every_leg_is_client_string_not_server_constant` new** — moved from T-6b's `check`, now also asserting the value is the ANDROID/IOS client UA, not `server.py`'s `USER_AGENT`) (closes AC-7, AC-9 in full, AC-1's duration plumbing, AC-2's default-language data, AC-18's fetch precondition, AC-20's outbound-header requirement, AC-21's `languageName` sanitization, AC-22's `Retry-After` handling, AC-23's request budget proved at the provider level)

## T-11 — `tools/list_transcript_tracks.py`
- after: T-5, T-7, T-9
- files: `plugins/youtube-transcript/plugin/server/tools/list_transcript_tracks.py`, `plugins/youtube-transcript/tests/test_tool_list_tracks.py`
- interface: consumes `TranscriptProvider`/`ProviderSession` (`FakeProvider`/`FakeSession`), `sort_tracks` from T-9, `estimate_characters` from T-5, `outcome_from_error` from T-3 · produces `handle(provider, deadline, args) -> ToolOutcome`
- acceptance: GIVEN `args["video"]` fails `provider.normalize_video_ref` WHEN `handle` runs THEN `status: "video_not_found"` and `provider.open` is called **zero** times (assert on `FakeProvider`'s call counter, not just the returned status — cycle 8 fix, closes AC-6/AC-8's untested "normalize before any request" requirement); otherwise calls `provider.open(...)` exactly once, never calls `session.fetch(...)`; GIVEN `session.listing.tracks` is empty WHEN `handle` runs THEN the outcome is `status: "no_transcript"` with `payload={"tracks": [], "videoDurationSeconds": session.listing.duration_seconds}` (now directly reachable since `open()` always succeeds and populates duration regardless), and the **built envelope** (not just the `ToolOutcome`) still contains `videoDurationSeconds` (T-7's frozen `FIELDS_BY_STATUS[no_transcript]` is what makes this reachable); caps returned tracks at 50 with `truncated=true` beyond that, sorted per T-9; **`estimated_characters` is exclusively computed here, cycle 4 fix** — `formats.estimate_characters(session.listing.duration_seconds)` is called **exactly once per `handle()` invocation, not once per track (cycle 8 fix)**, since its result depends only on `duration_seconds`, which is identical for every track of the same video — the single computed `{"text": ..., "srt": ..., "vtt": ...}` mapping (single-argument, matching T-5's declared signature exactly — cycle 6 fix, a stray extra-args ellipsis here previously suggested a different signature than T-5 declared) is reused across all tracks kept after the cap, each getting its own **new, response-only** `TrackDescriptor` copy carrying that mapping (the original, provider-returned descriptors in `session.listing.tracks` are never mutated and never re-passed to `session.fetch()` — this task calls `fetch()` zero times, so there is no membership-equality conflict with `fetch()`'s "member of `self.listing.tracks`" precondition); the response-only copies also carry `track_id` (already set by the provider, copied through unchanged) and `estimatedCharacters`; includes root-level `videoDurationSeconds`; GIVEN `provider.open(...)` raises `VideoNotFound` THEN the corresponding `ToolOutcome` via `outcome_from_error`; **the entire body of `handle()` is wrapped in a single `except DomainFailure -> outcome_from_error`, not only the `formats.estimate_characters` call site** (the prior acceptance only named the `formats.estimate_characters`-raises-mid-pass case, leaving `provider.open(...)` itself raising `DeadlineExpired` uncovered by any test; two separate GIVEN cases now: `FakeProvider.open()` raising `DeadlineExpired` directly, and `formats.estimate_characters` raising `DeadlineExpired` mid-pass — both map to `status: transport_error`, `isError: false`)
- check: `test_tool_list_tracks.py::{test_invalid_video_ref_returns_not_found_without_opening,test_open_called_once,test_never_calls_fetch,test_empty_tracks_maps_to_no_transcript_with_duration,test_envelope_retains_duration_on_no_transcript,test_track_count_cap_and_sort,test_estimated_characters_computed_once_not_per_track,test_original_listing_tracks_never_mutated,test_video_not_found,test_open_raises_deadline_expired_maps_to_transport_error,test_estimate_characters_raises_deadline_expired_mid_pass_maps_to_transport_error}` (closes AC-1, AC-6, AC-7, AC-8)

## T-12 — `tools/get_transcript.py`
- after: T-5, T-7, T-8, T-9
- files: `plugins/youtube-transcript/plugin/server/tools/get_transcript.py`, `plugins/youtube-transcript/tests/test_tool_get_transcript.py`
- interface: consumes `select_track`/`validate_languages` from T-9, `encode`/`count_pages_to` from T-5, `FORMATS` from T-2 (re-exported via `domain/`, cycle 7 — used for the `format` argument's AC-4 enum check), `tools.cursor.decode`/`encode` from T-8, `outcome_from_error` from T-3 · produces `handle(provider, deadline, args) -> ToolOutcome`
- acceptance: GIVEN a `video` argument (no `cursor`) failing `provider.normalize_video_ref` WHEN `handle` runs THEN `status: "video_not_found"` and `provider.open` is called **zero** times (assert on `FakeProvider`'s call counter — cycle 8 fix, mirrors T-11's equivalent case, closes AC-6/AC-8's untested "normalize before any request" requirement); GIVEN no `cursor` THEN `provider.open(...)` exactly once, `session.fetch(track, deadline)` exactly once; GIVEN `session.listing.tracks` is empty THEN `status: "no_transcript"` **and exactly 2 requests total, `session.fetch(...)` never called** (the empty-tracks path costs 2 requests, not 3); GIVEN `select_track` (T-9) finds no match for the requested `languages`/`trackId` THEN `status: "language_unavailable"` with `payload={"availableLanguages": [...]}` — the sorted, deduped `languageCode` list from `session.listing.tracks`, capped at 50 (AC-3, AC-1's shared cap; cycle 4 finding — previously unowned by any task, closed by T-7's new `FIELDS_BY_STATUS[language_unavailable]` freeze plus this task actually populating the field); GIVEN `select_track` finds a match THEN this task's outcome payload carries `resolved_track: TrackDescriptor` (the domain object `envelope.build()` consumes to construct the wire-shape `resolvedTrack: {languageCode, kind, sourceLanguageCode}`, mirroring how `tracks` is already threaded through as domain objects rather than a pre-built dict — cycle 4, AC-5); GIVEN a `cursor` THEN other args are ignored in favor of the cursor's values where both present; GIVEN `tools.cursor.decode` raises `CursorInvalid` (a malformed or forged cursor string) THEN `status: "upstream_changed"` — **covered by the single blanket catch below, cycle 6 simplification**, since `CursorInvalid(DomainFailure)` (T-8) is now a `DomainFailure` from the start, no special-cased second `except` needed; GIVEN a cursor's `videoId` mismatching the `video` argument THEN `upstream_changed`; GIVEN a cursor's `segmentIndex` exceeding the actually-fetched track's real segment count (phase 2, checked here since `tools/cursor.py` cannot know the real count at decode time) THEN `upstream_changed`; GIVEN a transcript longer than `MAX_PAGES` pages WHEN `handle` replays `formats.count_pages_to` from `segmentIndex=0` to the cursor's `segmentIndex` THEN `truncated=true`/`nextCursor=null` once the derived count reaches `MAX_PAGES` — this remains a full fetch+decode before the ceiling response, an accepted disclosed cost (plan.md Decisions Made — no cross-invocation caching, per AC-11's stateless-cursor requirement), not something this task tries to fast-path; **the entire body of `handle()` is wrapped in a single `except DomainFailure -> outcome_from_error`, not only the `formats.encode`/`count_pages_to` call site** (covers `ProviderError`, `DeadlineExpired`, and `CursorInvalid` uniformly — the prior acceptance only named the mid-pass-encode case, leaving `provider.open(...)`/`session.fetch(...)` raising `DeadlineExpired` directly uncovered by any test); four separate GIVEN cases, each mapping through `outcome_from_error` to its own correct status, `isError: false`: `FakeProvider.open()` raises `DeadlineExpired` (`transport_error`), `FakeSession.fetch()` raises `DeadlineExpired` (`transport_error`), `formats.encode`/`count_pages_to` raises `DeadlineExpired` mid-pass (`transport_error`), `tools.cursor.decode` raises `CursorInvalid` (`upstream_changed`)
- check: `test_tool_get_transcript.py::{test_invalid_video_ref_returns_not_found_without_opening,test_no_cursor_exactly_once_open_and_fetch,test_empty_tracks_no_transcript_exactly_two_requests_never_fetches,test_language_unavailable_includes_available_languages,test_resolved_track_in_payload_on_success,test_cursor_overrides_other_args,test_malformed_cursor_maps_to_upstream_changed,test_cursor_video_id_mismatch_upstream_changed,test_cursor_segment_index_exceeds_real_count_upstream_changed,test_max_pages_truncation_after_full_fetch,test_open_raises_deadline_expired_maps_to_transport_error,test_fetch_raises_deadline_expired_maps_to_transport_error,test_encode_raises_deadline_expired_mid_pass_maps_to_transport_error}` (closes AC-3's `availableLanguages`, AC-4, AC-5 in full incl. `resolvedTrack`, AC-7, AC-8, AC-11 in full)

## T-13b — `composition.py` + `server.py`
- after: T-10, T-11, T-12, T-13a
- files: `plugins/youtube-transcript/plugin/server/composition.py`, `server.py` (**incl. `SERVER_VERSION`/`USER_AGENT`, cycle 8 — see below**), `plugins/youtube-transcript/tests/test_composition.py`
- interface: consumes `InnertubeProvider` from T-10, both tools' `handle()` from T-11/T-12, `Registry`/`serve_stdio` from T-13a · produces, **split by file (`composition.py`'s `ALLOWED_EDGES` row omits `protocol/`/`tools/`, so `main()` and the handler-registration wiring are only legal in `server.py`)**: `composition.py` → `build_provider(*, sleep: Callable[[float],None] = time.sleep, jitter: Callable[[float,float],float] = random.uniform) -> TranscriptProvider` (called once, at startup; **`sleep`/`jitter` are injectable parameters, cycle 6 fix, passed straight through to `InnertubeProvider.__init__` — this is what lets T-14's end-to-end tests actually reach the injection seam T-6b declared on `net.fetch()` itself, closing the gap where the seam existed at the bottom of the stack but nothing above it exposed a way to supply it**), `build_deadline(clock: Callable[[], float] = time.monotonic) -> Deadline` (clock is an explicit injectable parameter — the alternative, a no-argument signature, gives this task's own per-call test nowhere to inject a clock without globally monkeypatching `time.monotonic`, which would also perturb the retry/backoff machinery running inside the same call); `server.py` → **`build_registry(provider: TranscriptProvider, *, clock: Callable[[], float] = time.monotonic) -> Registry`** (cycle 4 finding — a named function, not prose, is what actually gives the per-call clock-injection test somewhere concrete to call: it registers both tools' handlers as closures, each calling `build_deadline(clock=clock)` fresh per invocation) plus `main()`, which is `build_registry`'s only caller (with `clock` defaulted, i.e. `time.monotonic`) and wires `serve_stdio`; **`SERVER_VERSION = "0.27.0"`, `USER_AGENT = "youtube-transcript/0.27.0"` module-level constants in `server.py`, cycle 8** — previously unassigned to any task; the root `CLAUDE.md` Non-negotiable requires these alongside `plugin.json`/`marketplace.json`'s version, and `scripts/validate.sh --check-tag` greps for them by name at the path `plugin.json`'s `args` names, which is `server.py`
- acceptance: exactly one `build_provider(` call site outside `composition.py`/`tests/`; a subprocess smoke test running `server.py` with empty stdin succeeds with no `ModuleNotFoundError` for any stdlib name; GIVEN two sequential `tools/call` messages, dispatched through a `Registry` from `build_registry(provider, clock=fake_clock)` with `fake_clock` advanced past `TOOL_CALL_DEADLINE` between the two calls, WHEN both are dispatched THEN the second call still succeeds (proves `build_deadline()` is per-call, not bound once at registration, via `build_registry`'s concrete injection seam rather than global monkeypatching — closes cycle 2's "`Deadline` lifecycle across the T-13 split was unowned" finding); end-to-end, a real `tools/call` for `get_transcript` through the full dispatch stack (mocked transport) issues exactly 3 requests, and for `list_transcript_tracks` exactly 2
- check: `test_composition.py::{test_single_build_provider_call_site,test_build_registry_and_main_only_in_server_py,test_stdlib_shadowing_smoke_import,test_second_call_gets_fresh_deadline_via_build_registry_clock,test_end_to_end_request_count_get_transcript,test_end_to_end_request_count_list_tracks}` (closes AC-23 in full, AC-24's remaining wiring)

## T-14 — Cross-cutting AC tests
- after: T-13b
- files: `plugins/youtube-transcript/tests/test_no_file_writes.py`, `test_request_budget.py`, `test_status_sweep.py`, `plugins/youtube-transcript/tests/test_source_policy.py` (extend — this task's portion: write/exec-primitive bans)
- interface: consumes the fully wired server from T-13b · produces none
- acceptance: zero filesystem writes running both tools against fixtures in an empty temp dir; `test_source_policy.py` bans (as named tests, not a single combined grep) `open(...,'w'|'a')`/`Path.write_text`/`write_bytes`/`os.mkdir`/`os.makedirs`/`os.open`/`os.rename`/`os.replace`/`shutil.*`/`tempfile.*`, **and separately** `subprocess`/`os.system`/`os.popen`/`os.exec*`/`eval(`/`exec(`/`pickle`/`marshal`/`__import__`, both under `plugin/server/**`; GIVEN every one of 3 legs fails once (5xx, first attempt) and succeeds on retry (second attempt, fast — not a timeout) WHEN a non-paginated `get_transcript` runs end-to-end THEN **exactly** 6 requests are issued (not "≤6" — a weaker bound is satisfied by an implementation that never retries); the equivalent case for `list_transcript_tracks`'s 2 legs asserts **exactly** 4; **two timing cases**, both driven through `build_provider(sleep=fake_sleep, jitter=fixed_max_jitter)` (cycle 6 — the actual, concrete injection point, now that it exists end-to-end) rather than global monkeypatching: (a) **early-exit** — leg 1 fails all `HTTP_MAX_ATTEMPTS` attempts by timeout WHEN `get_transcript` runs THEN it fails at **exactly** `HTTP_TIMEOUT + backoff_delay + HTTP_TIMEOUT` (computed from the constants, not `≈31.25s` hardcoded — cycle 6 fix, the same class of fix already applied to case (b) below) with **exactly 2 requests issued**, never reaching legs 2/3; (b) **worst-case-still-succeeds** — all 3 legs fail their first attempt by timeout, and the retry attempt's mock responds at `t = HTTP_TIMEOUT - EPSILON` into the retry (`EPSILON = 0.01s`, an exact value under the injected fake clock, not an approximation) WHEN `get_transcript` runs THEN it succeeds at `3 × (HTTP_TIMEOUT + backoff_delay + (HTTP_TIMEOUT - EPSILON))` — computed from the same constants and the pinned max-jitter `backoff_delay`, not a hardcoded decimal — with exactly 6 requests; **both timing assertions use `math.isclose(actual, expected, rel_tol=0.0, abs_tol=1e-9)`, not bare `==`, with both tolerance arguments named explicitly, cycle 7 fix** (cycle 6 named only `abs_tol=1e-9`, leaving `math.isclose`'s non-zero default `rel_tol` implicitly in force alongside it — for values in this magnitude range `rel_tol` would dominate the comparison, silently widening the tolerance the task claimed to be pinning; stating `rel_tol=0.0` explicitly makes `abs_tol` the sole, actual tolerance in effect, matching the stated intent — the formula-computed expected value and the mock's own accumulated elapsed time are only bit-identical for one specific order of floating-point summation, which is why a tolerance is needed at all); **every timing assertion in this file uses an injected clock, never real elapsed time** (same reasoning as T-6b — these scenarios would otherwise cost real wall-clock minutes per run); every one of the 13 `Status` enum members is reached by at least one test across the whole suite; **the full `DomainFailure` totality check, cycle 7 fix — corrected count**: the prior wording ("`DomainFailure.__subclasses__()`, recursively, equals exactly the 10 `ProviderError` subclasses plus `DeadlineExpired`/`CursorInvalid`/`TrackFieldInvalid`") undercounts by one, confirmed by execution — `ProviderError` is itself a **direct** subclass of `DomainFailure` (alongside its 10 leaf subclasses, which are subclasses of `ProviderError`, not direct children of `DomainFailure`), so `DomainFailure.__subclasses__()` (non-recursive, one level) returns `{ProviderError, DeadlineExpired, CursorInvalid, TrackFieldInvalid}` — 4 members — while the **leaf** failure types (the ones `outcome_from_error` actually maps one-to-one to a `Status`) are `ProviderError.__subclasses__()`'s 10 plus the same 3 non-`ProviderError` direct children — 13 members total. The test asserts **both** counts explicitly, named separately (`DomainFailure.__subclasses__()` == 4 direct children; the full recursive leaf set == 13), instead of one conflated "the tree" assertion that silently picked one of the two without saying so; **the interface cross-check from T-3** (moved here, cycle 4): for every module under `plugin/server/**`, every imported symbol resolves (via AST, following re-exports) to a module the importing module's `ALLOWED_EDGES` row (or the same-package rule) actually permits — this is the same property T-3's placeholder check named against `tasks.md` prose, now checked against the real, fully-built import graph, where it is actually falsifiable; **AC-26 is closed here, against the real dispatch stack, not at T-7 — narrow-check fix (Y-9)**: T-7's `test_envelope.py` coverage of the reference parser (three adversarial fixtures) is real but only unit-level — it constructs its own `json.dumps(build_result)` string inside the test, which is a reconstruction of the wire format, not the wire format itself, and would stay green even if `protocol/dispatch.py`'s actual serialization diverged from that reconstruction (different `json.dumps` options, an extra wrapping layer, a carrier change). `test_ac26_reference_parser_against_real_dispatch_response`: GIVEN a stub `TranscriptProvider` configured to serve a `get_transcript` response whose caption text is, in turn, each of AC-26's three adversarial fixtures (forged boundary-shaped token, forged `contentNotice`-shaped string, partial-prefix collision) WHEN a real `tools/call` JSON-RPC message is dispatched through `handle_message` (the actual production path — `Registry.call()` → `envelope.build()` → `json.dumps()` → `content[0].text`) THEN the reference parser, given `response["result"]["content"][0]["text"]` verbatim (no test-side re-serialization), correctly reconstructs the untrusted region in all three cases — this is the test that actually discharges AC-26's "against the actual wire artifact a consumer receives" requirement; **AC-3's 11-language cap dispatched end-to-end, post-cycle-9 hostile-implementer-walkthrough finding**: `test_eleven_languages_rejected_as_domain_error_not_schema_error` — GIVEN a real `tools/call` JSON-RPC message for `get_transcript` with an 11-entry `languages` array WHEN dispatched through `handle_message` (the full `Registry`/`TOOL_SCHEMAS`-validation/`envelope.build()` path, not `T-9::validate_languages` called directly in isolation) THEN the response is `status: "language_unavailable"`, never a JSON-RPC `-32602` schema-validation error — this is the test that would catch a `maxItems` accidentally added to `TOOL_SCHEMAS` (see T-7), which T-9's own unit tests structurally cannot see since they never touch schema validation
- check: `test_no_file_writes.py`, `test_source_policy.py::{test_no_file_write_calls,test_no_execution_or_serialization_primitives}`, `test_request_budget.py::{test_exact_six_on_fast_failure_retry,test_exact_four_list_tracks_fast_failure,test_early_exit_leg1_timeout_two_requests,test_worst_case_all_legs_retry_succeeds_with_margin}`, `test_status_sweep.py`, `test_domain.py::{test_domain_failure_direct_children_count,test_domain_failure_leaf_set_matches_exactly}`, `test_import_boundaries.py::test_real_import_graph_matches_allowed_edges`, `test_dispatch.py::{test_ac26_reference_parser_against_real_dispatch_response,test_eleven_languages_rejected_as_domain_error_not_schema_error}` (**both new — Y-9 narrow-check fix and post-cycle-9 hostile-implementer-walkthrough finding respectively**) (closes AC-16 in full, AC-23's end-to-end request count with corrected exact/timing semantics, AC-17's full reachability sweep, the `ALLOWED_EDGES` self-check, the `DomainFailure` totality check, **AC-26 against the real wire artifact, AC-3's cap-as-domain-error at the dispatch layer**)

## T-15 — Live canary
- after: T-10, T-P3
- files: `plugins/youtube-transcript/tests/test_live_canary.py`
- interface: consumes `InnertubeProvider` from T-10, verified video IDs from T-P3 · produces none
- acceptance: skips by default (`YOUTUBE_TRANSCRIPT_LIVE_CANARY != "1"`); `ok` per pinned video passes; `blocked_by_provider`/`rate_limited`/`transport_error` skip loudly; `upstream_changed`/shape-mismatch/`no_transcript` on a known-good video fails immediately
- check: manual run with the env var set, plus T-P2's `workflow_dispatch` verification (closes AC-13)

## T-16 — Plugin docs + final `validate.sh`/coverage pass
- after: T-14, T-P4 (**T-P4 dependency new, narrow-check fix Y-16** — T-16's `plugin-dev:plugin-validator`/PLUGIN-STANDARDS §10 walk treats the spec as the authoritative contract; must run against a spec already synced to the plan's actual design)
- files: `plugins/youtube-transcript/CLAUDE.md`, `AGENTS.md`, `README.md`
- interface: consumes the final module list and T-P3's full measurement set · produces plugin documentation
- acceptance: `## Non-negotiables` section per convention; `README.md` documents stdio config, the ToS risk one-liner, **and the computed full-read cost** — T-P3's measured per-leg bytes/latency × `MAX_PAGES`, not a qualitative sentence; **and the no-caching pagination cost decision** (60 requests/20 full re-decodes/O(pages²) replay for a full `MAX_PAGES` read, per plan.md Decisions Made — cycle 3 finding: previously a docs disclosure with no Decisions Made row); merge is gated on **three** named measurement targets against T-P3 (cycle 3 adds the third), stated here verbatim: (1) "a 3-hour video's `srt` transcript fits within `MAX_PAGES` (20) pages at `MAX_PAGE_CHARS` (50,000)", checked against T-P3's **measured** srt chars/second (not the `CHARS_PER_SECOND × CUE_OVERHEAD_FACTOR = 45` estimate alone) — if it doesn't fit, `MAX_PAGE_CHARS` is raised before this task closes; (2) `HTTP_TIMEOUT` tuning as the falsifiable inequality it actually is (cycle 3 finding — the prior "3× measured p95" framing was unfalsifiable, since the clamp made "total ≤ budget" true for any `HTTP_TIMEOUT` value by truncating harder): `HTTP_MAX_ATTEMPTS × 3 legs × HTTP_TIMEOUT + 3 × max_backoff ≤ TOOL_CALL_DEADLINE - ENCODE_RESERVE` **and** `HTTP_TIMEOUT ≥ measured p95 of the slowest leg` — both sides checked, not just the ceiling; (3) T-P3's measured **p95** worst-case CPU-phase time (decode 100k segments + sanitize + `count_pages_to` replay to `MAX_PAGES` + final `encode`, **not** the 3-hour-fixture typical case) must satisfy **`p95(CPU phase) ≤ CPU_PHASE_BUDGET`, cycle 7 fix** (the prior gate, `p95(CPU phase) + HTTP_TIMEOUT ≤ ENCODE_RESERVE`, directly contradicted `ENCODE_RESERVE = 20s`'s own stated rationale of being sized for `20s` of CPU work — it left only `5s` of actual headroom, found by performance review as a self-contradiction, not an implementation bug; `ENCODE_RESERVE` is now derived as `HTTP_TIMEOUT + CPU_PHASE_BUDGET`, so the `+ HTTP_TIMEOUT` slow-drip reservation is already accounted for in `ENCODE_RESERVE`'s own definition and this gate checks the CPU budget directly against the constant it's actually about — see plan.md's Slow-drip defense section); (4) T-P3's measured actual MCP host tool-call timeout (see T-P3, **exponential-search-then-bisection with a downward branch and a cost bound, cycle 7 fix** — cycle 6's version had no branch if the first `180s` probe already failed, and could cost hours bisecting to `±5s` precision the gate doesn't need) must be `≥ TOOL_CALL_DEADLINE + HOST_TIMEOUT_MARGIN` (**`HOST_TIMEOUT_MARGIN = 30` — cycle 8, previously an unquantified `margin` — see T-2**) — `TOOL_CALL_DEADLINE = 180s` was chosen without reference to any measured external ceiling, and the same-repo `maven-mcp` plugin uses `30s` for its equivalent constant, so this is not a rubber-stamp check; if any of the four fails, the relevant constant (`MAX_PAGE_CHARS`, `HTTP_TIMEOUT`, `CPU_PHASE_BUDGET`, or `TOOL_CALL_DEADLINE`) is raised or lowered and the worked derivation in plan.md is re-run with the new value — **and, since `T-6b`'s and `T-14`'s tests assert the specific numeric consequences of the current constants (`31.25s`/`93.75s`/`51.25s` etc. — updated from the cycle-6 figures per `ENCODE_RESERVE`'s cycle-7 derivation), those tests' assertions (computed from the constants, per T-6b's/T-14's own `math.isclose`-based formulas, not separately hardcoded decimals) are re-verified against the new constant values before this task closes, not left silently stale** (cycle 4 finding, still the governing remediation shape — this task's own gate values, not "hardcoded assertions" that no longer exist as literal numbers in the test files themselves, since T-6b/T-14 already compute expected values from the constants rather than hardcoding them); `bash scripts/validate.sh --check-tag 0.27.0` passes (**cycle 8: `--check-tag` moved here from T-P1** — T-P1 runs before `server.py` exists, so its `--check-tag` invocation structurally could not verify `SERVER_VERSION`/`USER_AGENT`; T-16 runs after T-13b has created the file); coverage `fail_under` (≥75) satisfied; **`plugin-dev:plugin-validator` run against both `youtube-transcript` and `maven-mcp` (unified versioning makes this a two-plugin release from this change forward), result PASS or Minor-only for each, recorded in `progress.md` — cycle 8, root `CLAUDE.md`'s Publishing section requires this before every release and no task previously owned it**; **`docs/PLUGIN-STANDARDS.md` §10 pre-release checklist walked for both plugins, recorded in `progress.md` — cycle 8, same root-`CLAUDE.md` requirement, previously unowned**
- check: `bash scripts/validate.sh --check-tag 0.27.0`; `coverage report --rcfile=plugins/youtube-transcript/pyproject.toml`; `plugin-dev:plugin-validator` transcript for both plugins attached to `progress.md`; manual walk of `docs/PLUGIN-STANDARDS.md` §10 for both plugins, recorded in `progress.md`; manual check of all three T-P3-measurement gates against their explicit named targets (closes AC-12's plugin-side registration, the pagination-ceiling, timeout-tuning, and CPU-reserve disclosures)

---

## T-P1 — Versioning and repo-doc generalization
- after: none
- files: `plugins/youtube-transcript/plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `plugins/maven-mcp/plugin/.claude-plugin/plugin.json`, `plugins/maven-mcp/plugin/server/server.py`, `CLAUDE.md`, `AGENTS.md`, `README.md` (repo root)
- interface: consumes none · produces both plugins registered at `0.27.0`; generalized root docs
- acceptance: unchanged from cycle 1 — version fields consistent, plugin-count wording generalized, `youtube-transcript` README section added
- check: `bash scripts/validate.sh` (**cycle 8: `--check-tag` moved to T-16** — at T-P1, `plugins/youtube-transcript/plugin/server/server.py` does not exist yet, so a `--check-tag` run here silently no-ops on it via `[ -f "$script" ] || continue` rather than actually verifying anything, which is exactly the invisible-gap architecture/performance review found; T-P1 confirms `plugin.json`/`marketplace.json`'s version fields only, T-16 confirms `server.py`'s `SERVER_VERSION`/`USER_AGENT` once it exists); `grep -L "one plugin\|1 plugin (maven-mcp)" CLAUDE.md AGENTS.md` (closes AC-12)

## T-P2 — CI workflows
- after: none
- files: `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `.github/workflows/youtube-transcript-live-canary.yml` (new), `scripts/validate.sh`
- interface: consumes none · produces 4 named CI jobs, extended `release.yml` discovery, canary workflow with a cache-based consecutive-skip counter, `permissions:` check in `validate.sh`
- acceptance: unchanged from cycle 1 — 4 named jobs each with their own change detector; `working-directory: .` on the new `release.yml` step; canary has weekly schedule + `workflow_dispatch` + `permissions: contents: read` + the cache-key increment/reset/fail-at-3 counter (not just the workflow skeleton) + DEBUG logging never enabled; `validate.sh` flags a missing `permissions:` key
- check: `bash scripts/validate.sh`; one manual `workflow_dispatch` exercising the counter, verified by a human before merge (closes AC-25, contributes to AC-13)

## T-P3 — Network spike: allowlist confirmation, canary videos, size/latency/RSS measurement
- after: none
- files: `plugins/youtube-transcript/tests/fixtures/*` (scrubbed, committed — the durable carrier, since `swarm-report/` is gitignored), `swarm-report/research/youtube-transcript-size-measurements.md` (working notes)
- interface: consumes none · produces the confirmed host-allowlist set (feeds T-6a), ≥2 canary video IDs (feeds T-15), scrubbed fixtures (feeds T-6b/T-10), the full measurement set below (feeds T-16's **four** gates — cycle 4 adds the MCP-host-timeout gate)
- acceptance: encoded size in all 3 formats for 10-min/1-hour/3-hour videos, **plus the same size after `json.dumps(..., ensure_ascii=True)` (the default T-13a's wire carrier uses) — narrow-check fix (Y-14)**, since escaping non-ASCII (Cyrillic/CJK caption text, common for this plugin's audience) as `\uXXXX` inflates the encoded string roughly sixfold per affected character, and the pre-escaping "encoded size" figure alone understates what actually crosses the wire and counts against `MAX_PAGE_CHARS`; **per-leg latency (p50/p95) with TLS-handshake time recorded separately from time-to-first-byte, and the host each leg actually hit recorded** (feeds the connection-reuse deferral and the `HTTP_TIMEOUT` tuning rule — an aggregate number can't answer either); **byte transfer per leg, compressed and inflated** (spec-mandated, previously missing); **measured srt/vtt characters-per-second**, compared against the assumed `CHARS_PER_SECOND × CUE_OVERHEAD_FACTOR = 45` (feeds T-16's pagination-ceiling gate with a real number, not the estimate alone); **worst-case CPU-phase wall time and memory, targeting the `MAX_PAGES`-last-page worst case, not the 3-hour-fixture typical case**: synthesize a `MAX_SEGMENTS = 100,000`-segment transcript and time the *last page* of a `MAX_PAGES = 20` read specifically — `parse_xml_guarded` → `Segment` tuple → 100k `sanitize_text` calls → `count_pages_to` replay (up to `19 × MAX_PAGE_CHARS = 950,000` chars) → final-page `encode`, p50/p95 over ≥20 runs on the slowest CI matrix runner, not a dev laptop (feeds T-16's gate 3, `p95(CPU phase) ≤ CPU_PHASE_BUDGET`); memory measured via `resource.getrusage(RUSAGE_SELF).ru_maxrss` around the same pipeline (not `tracemalloc` alone — it tracks only CPython-allocator blocks, missing `expat`'s C-side buffers and the true OS-level footprint; `tracemalloc.get_traced_memory()` recorded alongside for attribution); **total requests/bytes/wall-time for a full 20-page paginated read** (feeds T-16's computed cost disclosure and the Decisions Made no-caching row); **the actual MCP host tool-call timeout** — run a stub tool handler that sleeps a controlled duration against the actual Claude Code / MCP client this plugin targets, and record the elapsed time at which the client gives up and the call is observed to fail on the caller's side (not the server's); **execution note, post-cycle-9 hostile-implementer-walkthrough finding**: the measuring process must observe this externally to the (potentially hung) probe call itself — run the probe in a subprocess or background task with the parent timing it from outside, not a synchronous in-process call that would block the measuring code along with the probe. If the implementing environment cannot do this (e.g. no subprocess capability, or no outbound network egress at all, making the whole live-measurement portion of this task unreachable), fall back to the already-documented `maven-mcp` precedent (`TOOL_DEADLINE = 30s`) as a conservative starting value and record gate 4 as **unmeasured**, not silently satisfied — the same fallback applies to this task's other live-network measurements: use `swarm-report/research/research-youtube-subtitles-plugin.md`'s already-captured empirical data (47+ live calls, `ANDROID`/`IOS` client context, ≥5 videos) as a documented, explicitly-flagged-as-not-independently-reverified starting point rather than blocking the entire Wave 0→1 critical path (T-6a/T-10 both `after: T-P3`) indefinitely; **measured with a single-probe-first strategy, cycle 8 fix, replacing cycle 7's exponential-search-then-bisection entirely** (cycle 8 performance review found gate 4 only ever asks one yes/no question — "is the ceiling `≥ TOOL_CALL_DEADLINE + HOST_TIMEOUT_MARGIN`?" — which an exponential search answers in one probe; cycle 7's up-to-6-doublings-then-bisect procedure existed to find a *precise* ceiling value nothing downstream consumes, at a real cost of up to several hours on the critical path this measurement already blocks (T-6a, T-10)): **first, probe at exactly `TOOL_CALL_DEADLINE + HOST_TIMEOUT_MARGIN` (`210s`)**. If it survives, gate 4 is satisfied — record "`≥ TOOL_CALL_DEADLINE + HOST_TIMEOUT_MARGIN`" and stop; no further probing, no bisection. If it fails, the ceiling is genuinely below where the plan needs it — fall back to a bounded downward search (`180 → 90 → 45 → ...`, capped at 6 halvings) purely to characterize how far below, recording either the first surviving value found ("`≥` that value, `<` `TOOL_CALL_DEADLINE + HOST_TIMEOUT_MARGIN`") or, if the cap is exhausted with no survivor, "`<` the smallest value probed" — either way, gate 4 is failed and `TOOL_CALL_DEADLINE` needs lowering before T-16 closes, which is the actionable outcome this measurement exists to produce; `TOOL_CALL_DEADLINE = 180s` was chosen without reference to any measured host ceiling, so an unmeasured value risks the client timing out and discarding a well-formed `transport_error` envelope this plugin would otherwise have produced (feeds T-16's gate 4); confirmed literal `*.googlevideo.com`-or-other host set, scrubbed fixture committed under `tests/fixtures/`, verified by an **allowlist-based** unit test, **cycle 8 fix** (was a denylist grepping for `visitorData`/`INNERTUBE_API_KEY`/`pot`/`signature`/`expire`/`sig` by name, which cannot catch a secret arriving under a new key name in a future recorded capture): the test asserts every top-level key, and every key nested under `captionTracks`/`playerConfig`, is a member of an explicitly enumerated **expected** key set — an unrecognized key fails the test instead of silently passing through
- check: `swarm-report/research/youtube-transcript-size-measurements.md` with all measurements recorded (size, handshake/transfer split with host attribution, byte transfer, chars/sec, worst-case-last-page decode/encode/replay time + `ru_maxrss`, full-read totals, MCP host timeout observation); the committed fixtures' scrub-check test passes (feeds T-6a/T-6b/T-10/T-15/T-16)

## T-P4 — Spec addendum: sync remaining plan/spec divergences
- after: none
- files: `docs/specs/2026-08-01-youtube-transcript.md`
- interface: consumes none · produces an updated spec matching this plan's actual, current design (not a new mechanism — a documentation sync)
- acceptance: **new, narrow-check fix (Y-16)** — the narrow check found `plan.md`'s divergence registry (`plan.md:307`, "six divergences tracked") had drifted from its own fast-follow addendum list (`plan.md:326`, four items, not matching), and neither list named the largest remaining gaps. This task closes them all in the spec, in one pass: (1) AC-20's literal `HTTP_TIMEOUT = 8`/`TOOL_CALL_DEADLINE = 45` (spec, multiple locations) updated to the plan's actual `15`/`180`; (2) AC-20's "`HTTP_TIMEOUT`/`TOOL_CALL_DEADLINE`/`ENCODE_RESERVE` all defined in `net/client.py`" updated to reflect their actual home in `domain/` (cycle 4/7/8) with `net/client.py` re-exporting `HTTP_TIMEOUT` only; (3) `CPU_PHASE_BUDGET`, `ENCODE_RESERVE`, `MIN_ATTEMPT_TIMEOUT`, `HOST_TIMEOUT_MARGIN`, `DEADLINE_CHECK_STRIDE` — named in the plan, absent from the spec entirely — added; (4) AC-21's control-character category set updated to include `Cs` (surrogates, cycle 8), matching the plan's `Cc ∪ Cf ∪ Cs ∪ {U+2028, U+2029}`; (5) the spec's Prerequisites tuning rule (`HTTP_TIMEOUT × 3 ≤ TOOL_CALL_DEADLINE − 15s`) — superseded by T-16's four measurement gates, but still stated in the spec as the governing rule — either removed or explicitly marked superseded, referencing T-16; (6) AC-11's placement of `MAX_SEGMENTS` in `formats/__init__.py` — updated to `domain/` (cycle 3); (7) **AC-1's track sort order, post-cycle-9 hostile-implementer-walkthrough finding** — AC-1 states tracks are "sorted by `(kind, languageCode)`," which read as a literal tuple-sort puts `"auto"` before `"manual"` alphabetically; T-9's `sort_tracks` implements the opposite (`manual: 0, auto: 1`) and is the intended, correctly-thought-through behavior (manual/human-created captions ranked above auto-generated is the sane default, and T-9's explicit-rank formulation was a deliberate design decision, not AC-1's likely-imprecise shorthand) — update AC-1 to "sorted manual-before-auto, then alphabetically by `languageCode` within each tier, matching T-9's `sort_tracks` rank." After this task, `plan.md:307`'s divergence count and `plan.md:326`'s addendum list are both updated to reflect zero remaining untracked divergences (or an explicit, current list if any are deliberately kept open). Does not block Wave 0-3, but must close before T-16 (whose `plugin-dev:plugin-validator`/§10 checklist walk implicitly treats the spec as the authoritative contract — an unsynced spec at that point would mean the release checklist is validated against a document describing a different system).
- check: `grep -n "HTTP_TIMEOUT = 8\|TOOL_CALL_DEADLINE = 45" docs/specs/2026-08-01-youtube-transcript.md` returns nothing; `grep -n "sorted by .(kind, languageCode)." docs/specs/2026-08-01-youtube-transcript.md` returns nothing (item 7); manual diff review of the seven items above against `plan.md`'s current text; `plan.md:307`/`plan.md:326` re-read for consistency after the spec edit
